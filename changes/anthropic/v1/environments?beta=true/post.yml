- change: added
  target: route
  breaking: false
  deprecated: false
  doc_only: false
  note: Introduces beta POST /v1/environments?beta=true to create a new environment with cloud networking and package configuration. Returns an Environment object on success; supports optional anthropic-beta and anthropic-version headers; 4XX returns an error response.
  paths: []
  date: '2026-04-08'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/879
- change: removed
  target: request
  breaking: true
  deprecated: false
  doc_only: false
  note: Removed cookie parameter 'sessionKey' from request; cookie-based session key is no longer accepted.
  paths:
    - path: parameters[name=sessionKey]
      before: '{"in":"cookie","name":"sessionKey","required":false,"schema":{"anyOf":[{"type":"string"},{"type":"null"}],"title":"Sessionkey"}}'
      after: 'null'
  date: '2026-04-09'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/882
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: true
  note: Updated anthropic-version header docs link to platform.claude.com
  paths:
    - path: parameters[1].description
      before: |-
        The version of the Claude API you want to use.

        Read more about versioning and our version history [here](https://docs.claude.com/en/api/versioning).
      after: |-
        The version of the Claude API you want to use.

        Read more about versioning and our version history [here](https://platform.claude.com/docs/en/api/versioning).
    - path: parameters[1].schema.description
      before: |-
        The version of the Claude API you want to use.

        Read more about versioning and our version history [here](https://docs.claude.com/en/api/versioning).
      after: |-
        The version of the Claude API you want to use.

        Read more about versioning and our version history [here](https://platform.claude.com/docs/en/api/versioning).
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/934
- change: changed
  target: response
  breaking: false
  deprecated: false
  doc_only: true
  note: Updated errors docs link in 4XX response description to platform.claude.com
  paths:
    - path: responses.4XX.description
      before: |-
        Error response.

        See our [errors documentation](https://docs.claude.com/en/api/errors) for more details.
      after: |-
        Error response.

        See our [errors documentation](https://platform.claude.com/docs/en/api/errors) for more details.
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/934
- change: added
  target: request
  breaking: false
  deprecated: false
  doc_only: false
  note: Added support for self_hosted config in create request (new discriminator mapping and schema option)
  paths:
    - path: components.schemas.BetaPublicEnvironmentCreateRequest.properties.config.anyOf[0].discriminator.mapping.self_hosted
      before: 'null'
      after: '#/components/schemas/BetaSelfHostedConfigParams'
    - path: components.schemas.BetaPublicEnvironmentCreateRequest.properties.config.anyOf[0].oneOf[1].$ref
      before: 'null'
      after: '#/components/schemas/BetaSelfHostedConfigParams'
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/934
- change: added
  target: request
  breaking: false
  deprecated: false
  doc_only: false
  note: Added optional 'scope' field to create request ('organization' | 'account' or null)
  paths:
    - path: components.schemas.BetaPublicEnvironmentCreateRequest.properties.scope
      before: 'null'
      after: '{"anyOf":[{"enum":["organization","account"],"type":"string"},{"type":"null"}],"description":"The visibility scope for this environment. ''organization'' makes the environment visible to all accounts. ''account'' restricts visibility to the owning account only. Only applicable for self-hosted environments. If not specified, defaults based on organization type.","title":"Scope"}'
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/934
- change: added
  target: response
  breaking: true
  deprecated: false
  doc_only: false
  note: Response 'config' now supports self_hosted environments (new discriminator mapping and schema)
  paths:
    - path: components.schemas.BetaEnvironment.properties.config.discriminator.mapping.self_hosted
      before: 'null'
      after: '#/components/schemas/BetaSelfHostedConfig'
    - path: components.schemas.BetaEnvironment.properties.config.oneOf[1].$ref
      before: 'null'
      after: '#/components/schemas/BetaSelfHostedConfig'
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/934
- change: changed
  target: response
  breaking: true
  deprecated: false
  doc_only: false
  note: Environment 'description' in response can now be null (type changed to string | null)
  paths:
    - path: components.schemas.BetaEnvironment.properties.description.type
      before: string
      after: 'null'
    - path: components.schemas.BetaEnvironment.properties.description.anyOf
      before: 'null'
      after: '[{"type":"string"},{"type":"null"}]'
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/934
- change: added
  target: response
  breaking: false
  deprecated: false
  doc_only: false
  note: Added 'scope' field to Environment response ('organization' | 'account')
  paths:
    - path: components.schemas.BetaEnvironment.properties.scope
      before: 'null'
      after: '{"description":"The visibility scope for this environment. ''organization'' means visible to all accounts. ''account'' means visible only to the owning account.","enum":["organization","account"],"title":"Scope","type":"string"}'
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/934
- change: changed
  target: response
  breaking: false
  deprecated: false
  doc_only: true
  note: Updated wording from BYOC to self-hosted in Environment description
  paths:
    - path: components.schemas.BetaEnvironment.description
      before: Unified Environment resource for both cloud and BYOC environments.
      after: Unified Environment resource for both cloud and self-hosted environments.
    - path: components.schemas.BetaEnvironment.properties.config.description
      before: Environment configuration (either Anthropic Cloud or BYOC)
      after: Environment configuration (either Anthropic Cloud or self-hosted)
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/934
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: true
  note: Clarified that packages require networking.allow_package_managers=true under limited networking in CloudConfigParams.packages description.
  paths:
    - path: components/schemas/BetaCloudConfigParams/properties/packages/description
      before: Package manager configuration. Omit on update to preserve the existing value.
      after: Package manager configuration. Under `limited` networking, requires `networking.allow_package_managers` to be `true`. Omit on update to preserve the existing value.
  date: '2026-08-26'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/959
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: true
  note: 'Expanded LimitedNetworkParams.allow_package_managers description: default applies on creation and must be true when packages are specified.'
  paths:
    - path: components/schemas/BetaLimitedNetworkParams/properties/allow_package_managers/description
      before: Permits outbound access to public package registries (PyPI, npm, etc.) beyond those listed in the `allowed_hosts` array. Defaults to `false`.
      after: Permits outbound access to public package registries (PyPI, npm, etc.) beyond those listed in the `allowed_hosts` array. Defaults to `false` on creation. Must be `true` when `packages` are specified.
  date: '2026-08-26'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/959
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: true
  note: Added note to PackagesParams description that limited networking requires networking.allow_package_managers=true.
  paths:
    - path: components/schemas/BetaPackagesParams/description
      before: |-
        Specify packages (and optionally their versions) available in this environment.

        When versioning, use the version semantics relevant for the package manager, e.g. for `pip` use `package==1.0.0`. You are responsible for validating the package and version exist. Unversioned installs the latest.
      after: |-
        Specify packages (and optionally their versions) available in this environment.

        When versioning, use the version semantics relevant for the package manager, e.g. for `pip` use `package==1.0.0`. You are responsible for validating the package and version exist. Unversioned installs the latest.

        Under `limited` networking, requires `networking.allow_package_managers` to be `true`.
  date: '2026-08-26'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/959
