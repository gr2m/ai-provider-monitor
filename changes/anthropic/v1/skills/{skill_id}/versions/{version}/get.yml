- change: removed
  target: response
  breaking: false
  deprecated: false
  doc_only: false
  note: Removed enum constraint from ErrorResponse.type (was limited to ["error"]).
  paths:
    - path: responses.4XX.content.application/json.schema.properties.type.enum
      before: '["error"]'
      after: 'null'
  date: '2026-02-20'
  diff_url: https://github.com/gr2m/ai-provider-api-changes/pull/574
- change: removed
  target: response
  breaking: false
  deprecated: false
  doc_only: false
  note: Removed enum constraints from each error subtype's type field in 4XX responses.
  paths:
    - path: responses.4XX.content.application/json.schema.properties.error.oneOf[0].properties.type.enum
      before: '["invalid_request_error"]'
      after: 'null'
    - path: responses.4XX.content.application/json.schema.properties.error.oneOf[1].properties.type.enum
      before: '["authentication_error"]'
      after: 'null'
    - path: responses.4XX.content.application/json.schema.properties.error.oneOf[2].properties.type.enum
      before: '["billing_error"]'
      after: 'null'
    - path: responses.4XX.content.application/json.schema.properties.error.oneOf[3].properties.type.enum
      before: '["permission_error"]'
      after: 'null'
    - path: responses.4XX.content.application/json.schema.properties.error.oneOf[4].properties.type.enum
      before: '["not_found_error"]'
      after: 'null'
    - path: responses.4XX.content.application/json.schema.properties.error.oneOf[5].properties.type.enum
      before: '["rate_limit_error"]'
      after: 'null'
    - path: responses.4XX.content.application/json.schema.properties.error.oneOf[6].properties.type.enum
      before: '["timeout_error"]'
      after: 'null'
    - path: responses.4XX.content.application/json.schema.properties.error.oneOf[7].properties.type.enum
      before: '["api_error"]'
      after: 'null'
    - path: responses.4XX.content.application/json.schema.properties.error.oneOf[8].properties.type.enum
      before: '["overloaded_error"]'
      after: 'null'
  date: '2026-02-20'
  diff_url: https://github.com/gr2m/ai-provider-api-changes/pull/574
- change: removed
  target: request
  breaking: true
  deprecated: false
  doc_only: false
  note: Removed anthropic-beta header parameter for opting into beta features.
  paths:
    - path: parameters[name=anthropic-beta]
      before: '{"name":"anthropic-beta","in":"header"}'
      after: 'null'
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/934
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: true
  note: Path parameter version now documents accepting a version ID or 'latest'; timestamp addressing tied to a specific beta.
  paths:
    - path: parameters[name=version].description
      before: |-
        Version identifier for the skill.

        Each version is identified by a Unix epoch timestamp (e.g., "1759178010641129").
      after: |-
        Identifies the skill version: a version ID, or — where the endpoint accepts it — the literal `latest` for the skill's most recent version.

        Requests carrying the `skills-2025-10-02` beta header address versions by their Unix epoch timestamp instead (e.g., "1759178010641129").
    - path: parameters[name=version].schema.description
      before: |-
        Version identifier for the skill.

        Each version is identified by a Unix epoch timestamp (e.g., "1759178010641129").
      after: |-
        Identifies the skill version: a version ID, or — where the endpoint accepts it — the literal `latest` for the skill's most recent version.

        Requests carrying the `skills-2025-10-02` beta header address versions by their Unix epoch timestamp instead (e.g., "1759178010641129").
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/934
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: true
  note: Updated docs link for anthropic-version header.
  paths:
    - path: parameters[name=anthropic-version].description
      before: |-
        The version of the Claude API you want to use.

        Read more about versioning and our version history [here](https://docs.claude.com/en/api/versioning).
      after: |-
        The version of the Claude API you want to use.

        Read more about versioning and our version history [here](https://platform.claude.com/docs/en/api/versioning).
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/934
- change: removed
  target: response
  breaking: true
  deprecated: false
  doc_only: false
  note: Removed directory from the 200 response schema and from required fields.
  paths:
    - path: responses.200.content.application/json.schema.properties.directory
      before: '{"type":"string","title":"Directory","description":"Directory name of the skill version.\n\nThis is the top-level directory name that was extracted from the uploaded files."}'
      after: 'null'
    - path: responses.200.content.application/json.schema.required[?=="directory"]
      before: directory
      after: 'null'
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/934
- change: removed
  target: response
  breaking: true
  deprecated: false
  doc_only: false
  note: Removed version from the 200 response schema and from required fields.
  paths:
    - path: responses.200.content.application/json.schema.properties.version
      before: '{"type":"string","title":"Version","description":"Version identifier for the skill.\n\nEach version is identified by a Unix epoch timestamp (e.g., \"1759178010641129\")."}'
      after: 'null'
    - path: responses.200.content.application/json.schema.required[?=="version"]
      before: version
      after: 'null'
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/934
- change: added
  target: response
  breaking: false
  deprecated: false
  doc_only: false
  note: created_at now declares format date-time.
  paths:
    - path: responses.200.content.application/json.schema.properties.created_at.format
      before: 'null'
      after: date-time
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/934
- change: added
  target: response
  breaking: false
  deprecated: false
  doc_only: false
  note: type is now constrained with const "skill_version".
  paths:
    - path: responses.200.content.application/json.schema.properties.type.const
      before: 'null'
      after: skill_version
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/934
- change: changed
  target: response
  breaking: false
  deprecated: false
  doc_only: true
  note: Updated created_at description (now refers to when the skill was created).
  paths:
    - path: responses.200.content.application/json.schema.properties.created_at.description
      before: ISO 8601 timestamp of when the skill version was created.
      after: ISO 8601 timestamp of when the skill was created.
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/934
- change: changed
  target: response
  breaking: false
  deprecated: false
  doc_only: true
  note: Updated id, name, and skill_id descriptions in the 200 response schema.
  paths:
    - path: responses.200.content.application/json.schema.properties.id.description
      before: |-
        Unique identifier for the skill version.

        The format and length of IDs may change over time.
      after: |-
        Unique identifier for this Skill Version. The id addresses the version in
        paths and pins it in references.
    - path: responses.200.content.application/json.schema.properties.name.description
      before: |-
        Human-readable name of the skill version.

        This is extracted from the SKILL.md file in the skill upload.
      after: |-
        The Skill's immutable kebab-case slug, set at creation from the first
        upload's SKILL.md frontmatter `name` (or its enclosing directory). Every
        later upload must resolve to the same value. Also the top-level directory
        of the Skill's mounted files and the base name of a downloaded archive.
    - path: responses.200.content.application/json.schema.properties.skill_id.description
      before: Identifier for the skill that this version belongs to.
      after: |-
        Unique identifier for the skill.

        The format and length of IDs may change over time.
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/934
- change: changed
  target: response
  breaking: false
  deprecated: false
  doc_only: true
  note: Updated docs link in 4XX error response description.
  paths:
    - path: responses.4XX.description
      before: |-
        Error response.

        See our [errors documentation](https://docs.claude.com/en/api/errors) for more details.
      after: |-
        Error response.

        See our [errors documentation](https://platform.claude.com/docs/en/api/errors) for more details.
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/934
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: true
  note: Clarified the 'version' path parameter description to always allow the literal 'latest'.
  paths:
    - path: parameters[1].description
      before: |-
        Identifies the skill version: a version ID, or — where the endpoint accepts it — the literal `latest` for the skill's most recent version.

        Requests carrying the `skills-2025-10-02` beta header address versions by their Unix epoch timestamp instead (e.g., "1759178010641129").
      after: |-
        Identifies the skill version: a version ID, or the literal `latest` for the skill's most recent version.

        Requests carrying the `skills-2025-10-02` beta header address versions by their Unix epoch timestamp instead (e.g., "1759178010641129").
  date: '2026-08-26'
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: true
  note: Clarified the 'version' parameter schema description to always allow 'latest'.
  paths:
    - path: parameters[1].schema.description
      before: |-
        Identifies the skill version: a version ID, or — where the endpoint accepts it — the literal `latest` for the skill's most recent version.

        Requests carrying the `skills-2025-10-02` beta header address versions by their Unix epoch timestamp instead (e.g., "1759178010641129").
      after: |-
        Identifies the skill version: a version ID, or the literal `latest` for the skill's most recent version.

        Requests carrying the `skills-2025-10-02` beta header address versions by their Unix epoch timestamp instead (e.g., "1759178010641129").
  date: '2026-08-26'
