- change: added
  target: route
  breaking: false
  deprecated: false
  doc_only: false
  note: Added deprecated Admin API endpoint GET /v1/organizations/tunnels?beta=true to list organization tunnels with pagination, workspace_id filtering, and optional include_archived; superseded by /v1/tunnels.
  paths: []
  date: '2026-08-26'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/959
- change: changed
  target: request
  breaking: true
  deprecated: false
  doc_only: false
  note: The anthropic-beta header is now required (was optional).
  paths:
    - path: parameters[name=anthropic-beta].required
      before: 'false'
      after: 'true'
  date: '2026-09-01'
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: true
  note: Updated anthropic-beta header description to require mcp-tunnels-2026-05-19 instead of generic optional/multi-beta phrasing.
  paths:
    - path: parameters[name=anthropic-beta].description
      before: |-
        Optional header to specify the beta version(s) you want to use.

        To use multiple betas, use a comma separated list like `beta1,beta2` or specify the header multiple times for each beta.
      after: 'This endpoint is in beta: requests must send `mcp-tunnels-2026-05-19` in this header.'
    - path: parameters[name=anthropic-beta].schema.description
      before: |-
        Optional header to specify the beta version(s) you want to use.

        To use multiple betas, use a comma separated list like `beta1,beta2` or specify the header multiple times for each beta.
      after: 'This endpoint is in beta: requests must send `mcp-tunnels-2026-05-19` in this header.'
  date: '2026-09-01'
