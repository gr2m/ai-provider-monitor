- change: added
  target: route
  breaking: false
  deprecated: false
  doc_only: false
  note: 'Deprecated Admin API endpoint to list certificates registered on a tunnel. Archived certs excluded by default (use include_archived=true to include). Not paginated (tunnels have at most two active certs). Superseded by /v1/tunnels with anthropic-beta: mcp-tunnels-2026-06-22 and WIF scope workspace:manage_tunnels.'
  paths: []
  date: '2026-08-26'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/959
- change: changed
  target: request
  breaking: true
  deprecated: false
  doc_only: false
  note: '''anthropic-beta'' header is now required (was optional).'
  paths:
    - path: parameters[4].required
      before: 'false'
      after: 'true'
  date: '2026-09-01'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/982
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: true
  note: Updated anthropic-beta header docs to require value `mcp-tunnels-2026-05-19`.
  paths:
    - path: parameters[4].description
      before: |-
        Optional header to specify the beta version(s) you want to use.

        To use multiple betas, use a comma separated list like `beta1,beta2` or specify the header multiple times for each beta.
      after: 'This endpoint is in beta: requests must send `mcp-tunnels-2026-05-19` in this header.'
    - path: parameters[4].schema.description
      before: |-
        Optional header to specify the beta version(s) you want to use.

        To use multiple betas, use a comma separated list like `beta1,beta2` or specify the header multiple times for each beta.
      after: 'This endpoint is in beta: requests must send `mcp-tunnels-2026-05-19` in this header.'
  date: '2026-09-01'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/982
