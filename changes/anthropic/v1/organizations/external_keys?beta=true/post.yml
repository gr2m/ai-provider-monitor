- change: added
  target: route
  breaking: false
  deprecated: false
  doc_only: false
  note: Creates a CMEK external key configuration for the caller's organization (AWS, GCP, or Azure KMS) and returns the created config.
  paths: []
  date: '2026-08-26'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/959
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: true
  note: Updated AWS kms_arn description to clarify only single-Region, in-account ARNs are accepted; cross-account, multi-Region, and alias ARNs are rejected.
  paths:
    - path: components/schemas/BetaAwsExternalKeyConfig/properties/kms_arn/description
      before: Full ARN of the AWS KMS key.
      after: Full ARN of the AWS KMS key. On Claude Platform on AWS the key must be a single-Region key in your organization's own AWS account; cross-account keys, multi-Region keys, and alias ARNs are rejected.
  date: '2026-09-01'
- change: changed
  target: response
  breaking: false
  deprecated: false
  doc_only: true
  note: Updated AWS kms_arn description to clarify only single-Region, in-account ARNs are accepted; cross-account, multi-Region, and alias ARNs are rejected.
  paths:
    - path: components/schemas/BetaAwsExternalKeyConfig/properties/kms_arn/description
      before: Full ARN of the AWS KMS key.
      after: Full ARN of the AWS KMS key. On Claude Platform on AWS the key must be a single-Region key in your organization's own AWS account; cross-account keys, multi-Region keys, and alias ARNs are rejected.
  date: '2026-09-01'
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: true
  note: Clarified AWS role_arn deprecation text and access path; field remains deprecated and ignored.
  paths:
    - path: components/schemas/BetaAwsExternalKeyConfig/properties/role_arn/description
      before: IAM role ARN. Deprecated — Anthropic reaches the KMS key via a managed intermediate role; this field is ignored.
      after: IAM role ARN. Deprecated — Anthropic reaches the KMS key through its own intermediate role (or, on Claude Platform on AWS, with credentials AWS issues for the Workspace); this field is ignored.
  date: '2026-09-01'
- change: changed
  target: response
  breaking: false
  deprecated: false
  doc_only: true
  note: Clarified AWS role_arn deprecation text and access path; field remains deprecated and ignored.
  paths:
    - path: components/schemas/BetaAwsExternalKeyConfig/properties/role_arn/description
      before: IAM role ARN. Deprecated — Anthropic reaches the KMS key via a managed intermediate role; this field is ignored.
      after: IAM role ARN. Deprecated — Anthropic reaches the KMS key through its own intermediate role (or, on Claude Platform on AWS, with credentials AWS issues for the Workspace); this field is ignored.
  date: '2026-09-01'
