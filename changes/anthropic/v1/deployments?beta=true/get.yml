- change: added
  target: route
  breaking: false
  deprecated: false
  doc_only: false
  note: Introduces beta endpoint GET /v1/deployments?beta=true to list managed agent deployments with pagination and filters (agent_id, status, created_at range) and optional inclusion of archived items. Returns a paginated data array and next_page cursor.
  paths: []
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/934
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: true
  note: Formatted the status query parameter description (added backticks around values and parameter name).
  paths:
    - path: parameters[?(@.name=='status')].description
      before: 'Filter by status: active or paused. Omit for both. To include archived deployments, use include_archived instead; the two cannot be combined.'
      after: 'Filter by status: `active` or `paused`. Omit for both. To include archived deployments, use `include_archived` instead; the two cannot be combined.'
  date: '2026-09-01'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/982
- change: changed
  target: response
  breaking: false
  deprecated: false
  doc_only: true
  note: Updated description of paused_reason to format status references with backticks.
  paths:
    - path: components/schemas/BetaManagedAgentsDeployment/properties/paused_reason/description
      before: Why the deployment is paused. Non-null exactly when status is paused; null otherwise.
      after: Why the deployment is `paused`. Non-null exactly when `status` is `paused`; null otherwise.
  date: '2026-09-01'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/982
- change: changed
  target: response
  breaking: false
  deprecated: false
  doc_only: true
  note: Formatted enum values in MemoryStoreResourceConfig.access description with backticks.
  paths:
    - path: components/schemas/BetaManagedAgentsMemoryStoreResourceConfig/properties/access/description
      before: Access mode for the mounted store. Defaults to read_write. read_only mounts the store as a read-only filesystem.
      after: Access mode for the mounted store. Defaults to `read_write`. `read_only` mounts the store as a read-only filesystem.
  date: '2026-09-01'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/982
