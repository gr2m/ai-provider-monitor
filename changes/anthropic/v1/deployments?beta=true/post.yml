- change: added
  target: route
  breaking: false
  deprecated: false
  doc_only: false
  note: Introduces POST /v1/deployments?beta=true to create a Managed Agents deployment, binding an agent to an environment, initial events, resources, optional schedule/budget, and vault credentials; returns a Deployment.
  paths: []
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/934
- change: changed
  target: response
  breaking: false
  deprecated: false
  doc_only: true
  note: Updated description for deployment.paused_reason to add code formatting and clarify relation to status.
  paths:
    - path: components/schemas/BetaManagedAgentsDeployment/properties/paused_reason/description
      before: Why the deployment is paused. Non-null exactly when status is paused; null otherwise.
      after: Why the deployment is `paused`. Non-null exactly when `status` is `paused`; null otherwise.
  date: '2026-09-01'
- change: changed
  target: response
  breaking: false
  deprecated: false
  doc_only: true
  note: Updated description for memory store access mode to add code formatting for values.
  paths:
    - path: components/schemas/BetaManagedAgentsMemoryStoreResourceConfig/properties/access/description
      before: Access mode for the mounted store. Defaults to read_write. read_only mounts the store as a read-only filesystem.
      after: Access mode for the mounted store. Defaults to `read_write`. `read_only` mounts the store as a read-only filesystem.
  date: '2026-09-01'
