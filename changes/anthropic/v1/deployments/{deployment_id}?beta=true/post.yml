- change: added
  target: route
  breaking: false
  deprecated: false
  doc_only: false
  note: Adds a beta endpoint to update a Managed Agents deployment by ID. Supports partial updates (agent, budget, description, environment_id, initial_events, metadata, name, resources, schedule, vault_ids) and returns the updated deployment object.
  paths: []
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/934
- change: changed
  target: response
  breaking: false
  deprecated: false
  doc_only: true
  note: Updated description of deployment.paused_reason to clarify with backticked status values.
  paths:
    - path: components.schemas.BetaManagedAgentsDeployment.properties.paused_reason.description
      before: Why the deployment is paused. Non-null exactly when status is paused; null otherwise.
      after: Why the deployment is `paused`. Non-null exactly when `status` is `paused`; null otherwise.
  date: '2026-09-01'
- change: changed
  target: response
  breaking: false
  deprecated: false
  doc_only: true
  note: Updated description of memory_store access mode to format enum values with backticks.
  paths:
    - path: components.schemas.BetaManagedAgentsMemoryStoreResourceConfig.properties.access.description
      before: Access mode for the mounted store. Defaults to read_write. read_only mounts the store as a read-only filesystem.
      after: Access mode for the mounted store. Defaults to `read_write`. `read_only` mounts the store as a read-only filesystem.
  date: '2026-09-01'
