- change: added
  target: route
  breaking: false
  deprecated: false
  doc_only: false
  note: Introduces beta GET /v1/deployment_runs to list deployment run records. Supports cursor pagination (limit, page) and filters (deployment_id, trigger_type, has_error, created_at[gte|lte|gt|lt]). Returns most recent runs first with data[] and next_page; 400 for invalid/expired page cursors and 200 with empty data for non‑existent deployment_id.
  paths: []
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/934
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: true
  note: Parameter 'page' description updated to include code formatting for next_page.
  paths:
    - path: parameters[name=page].description
      before: Opaque pagination cursor. Pass next_page from the previous response. Invalid or expired cursors return 400.
      after: Opaque pagination cursor. Pass `next_page` from the previous response. Invalid or expired cursors return 400.
  date: '2026-09-01'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/982
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: true
  note: Parameter 'deployment_id' description updated (code formatting added).
  paths:
    - path: parameters[name=deployment_id].description
      before: Filter to a specific deployment. Omit to list across all deployments in the workspace. Filtering by a non-existent deployment_id returns 200 with empty data.
      after: Filter to a specific deployment. Omit to list across all deployments in the workspace. Filtering by a non-existent `deployment_id` returns 200 with empty data.
  date: '2026-09-01'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/982
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: true
  note: Parameter 'has_error' description updated to add code formatting for field names.
  paths:
    - path: parameters[name=has_error].description
      before: 'Filter: true for runs with non-null error, false for runs with non-null session_id. Omit for all.'
      after: 'Filter: true for runs with non-null `error`, false for runs with non-null `session_id`. Omit for all.'
  date: '2026-09-01'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/982
- change: changed
  target: response
  breaking: false
  deprecated: false
  doc_only: true
  note: List response schema description updated to format created_at with code styling.
  paths:
    - path: components.schemas.BetaManagedAgentsListDeploymentRunsData.description
      before: Paginated list of deployment runs. Sorted by created_at descending (most recent first).
      after: Paginated list of deployment runs. Sorted by `created_at` descending (most recent first).
  date: '2026-09-01'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/982
- change: changed
  target: response
  breaking: false
  deprecated: false
  doc_only: true
  note: DeploymentRun.error description updated to code-format referenced fields.
  paths:
    - path: components.schemas.BetaManagedAgentsDeploymentRun.properties.error.description
      before: Populated on creation failure. Null on success. Exactly one of session_id or error is non-null.
      after: Populated on creation failure. Null on success. Exactly one of `session_id` or `error` is non-null.
  date: '2026-09-01'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/982
- change: changed
  target: response
  breaking: false
  deprecated: false
  doc_only: true
  note: DeploymentRun.session_id description updated to code-format referenced fields.
  paths:
    - path: components.schemas.BetaManagedAgentsDeploymentRun.properties.session_id.description
      before: Populated on success. Null on creation failure. Exactly one of session_id or error is non-null.
      after: Populated on success. Null on creation failure. Exactly one of `session_id` or `error` is non-null.
  date: '2026-09-01'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/982
- change: changed
  target: response
  breaking: false
  deprecated: false
  doc_only: true
  note: ScheduleTriggerContext.scheduled_at description updated to code-format field names.
  paths:
    - path: components.schemas.BetaManagedAgentsScheduleTriggerContext.properties.scheduled_at.description
      before: The UTC instant at which the cron expression matched in the configured timezone, before jitter is applied. At most one run is recorded per (deployment_id, scheduled_at) pair.
      after: The UTC instant at which the cron expression matched in the configured timezone, before jitter is applied. At most one run is recorded per (`deployment_id`, `scheduled_at`) pair.
  date: '2026-09-01'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/982
