- change: added
  target: route
  breaking: false
  deprecated: false
  doc_only: false
  note: Introduces a beta endpoint to update a memory by ID within a memory store. Supports updating content and/or renaming path, optional optimistic precondition (content_sha256), and view=basic|full. Returns the updated memory object; conflicts and precondition failures return 409.
  paths: []
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/934
- change: changed
  target: response
  breaking: false
  deprecated: false
  doc_only: true
  note: Updated documentation for BetaManagedAgentsMemory.memory_version_id description (removed the word 'full' from 'Enumerate the full history').
  paths:
    - path: components/schemas/BetaManagedAgentsMemory/properties/memory_version_id/description
      before: ID of the `memory_version` representing this memory's current content (a `memver_...` value). This is the authoritative head pointer; `memory_version` objects do not carry an `is_latest` flag, so compare against this field instead. Enumerate the full history via [List memory versions](/en/api/beta/memory_stores/memory_versions/list).
      after: ID of the `memory_version` representing this memory's current content (a `memver_...` value). This is the authoritative head pointer; `memory_version` objects do not carry an `is_latest` flag, so compare against this field instead. Enumerate the history via [List memory versions](/en/api/beta/memory_stores/memory_versions/list).
  date: '2026-08-26'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/959
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: true
  note: 'Clarified path validation: now explicitly disallows Unicode line/paragraph separators U+2028/U+2029.'
  paths:
    - path: /components/schemas/BetaManagedAgentsUpdateMemoryParams/properties/path/description
      before: New path for the memory (a rename). Must start with `/`, contain at least one non-empty segment, and be at most 1,024 bytes. Must not contain empty segments, `.` or `..` segments, control or format characters, and must be NFC-normalized. Paths are case-sensitive. The memory's `id` is preserved across renames. Omit to leave the path unchanged.
      after: New path for the memory (a rename). Must start with `/`, contain at least one non-empty segment, and be at most 1,024 bytes. Must not contain empty segments, `.` or `..` segments, control or format characters, or the Unicode line and paragraph separators (U+2028, U+2029), and must be NFC-normalized. Paths are case-sensitive. The memory's `id` is preserved across renames. Omit to leave the path unchanged.
  date: '2026-09-01'
