- change: removed
  target: response
  breaking: true
  deprecated: false
  doc_only: false
  note: Removed `error` property from 200 OK response body (QueueJobResponse).
  paths:
    - path: components/schemas/QueueJobResponse/properties/error
      before: '{"$ref":"#/components/schemas/QueueError"}'
      after: 'null'
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/933
- change: changed
  target: response
  breaking: false
  deprecated: false
  doc_only: false
  note: '`requestId` is now required in QueueJobResponse.'
  paths:
    - path: components/schemas/QueueJobResponse/required
      before: 'null'
      after: '["requestId"]'
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/933
- change: changed
  target: response
  breaking: false
  deprecated: false
  doc_only: true
  note: Added description to QueueJobResponse schema.
  paths:
    - path: components/schemas/QueueJobResponse/description
      before: 'null'
      after: Response returned after queueing a job.
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/933
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: true
  note: Clarified QueueJobRequest and its `info` field descriptions.
  paths:
    - path: components/schemas/QueueJobRequest/description
      before: 'null'
      after: Request to submit a job to the queue.
    - path: components/schemas/QueueJobRequest/properties/info/description
      before: |
        Arbitrary JSON metadata stored with the job and returned in status
        responses. The model and system may add or update keys during
        processing.
      after: |
        Arbitrary JSON metadata stored with the job. Returned in status
        responses, where the model and system may have added or modified
        keys (e.g. progress).
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/933
- change: changed
  target: response
  breaking: false
  deprecated: false
  doc_only: true
  note: Added description to QueueError schema.
  paths:
    - path: components/schemas/QueueError/description
      before: 'null'
      after: Error response returned by queue endpoints.
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/933
