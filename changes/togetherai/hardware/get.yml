- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: true
  note: Updated model query param description to include link to dedicated models; example updated.
  paths:
    - path: parameters[0].schema.description
      before: |
        Filter hardware configurations by model compatibility. When provided,
        the response includes availability status for each compatible configuration.
      after: |
        Filter hardware configurations by model compatibility. When provided,
        the response includes availability status for each compatible configuration.
        [See all of Together AI's dedicated models](https://docs.together.ai/docs/dedicated-models)
    - path: parameters[0].schema.example
      before: meta-llama/Llama-3-70b-chat-hf
      after: deepseek-ai/DeepSeek-R1
  date: '2026-03-17'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/796
- change: removed
  target: response
  breaking: false
  deprecated: false
  doc_only: false
  note: Removed JSON Schema 'format' annotations from numeric fields in response schemas (cents_per_minute, gpu_memory, gpu_count).
  paths:
    - path: components/schemas/EndpointPricing/properties/cents_per_minute/format
      before: float
      after: 'null'
    - path: components/schemas/HardwareSpec/properties/gpu_memory/format
      before: float
      after: 'null'
    - path: components/schemas/HardwareSpec/properties/gpu_count/format
      before: int32
      after: 'null'
  date: '2026-08-24'
