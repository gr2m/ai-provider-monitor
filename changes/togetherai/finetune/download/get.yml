- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: true
  note: Updated the description of the checkpoint_step query parameter to clarify that 0 or omission downloads the final model and to reference the list checkpoints response.
  paths:
    - path: parameters[1].schema.description
      before: Specifies step number for checkpoint to download. Ignores `checkpoint` value if set.
      after: Specifies the checkpoint step to download from the list checkpoints response. A final checkpoint step downloads the final model; 0 or omitted downloads the final model by default. Ignores `checkpoint` value if set.
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/933
