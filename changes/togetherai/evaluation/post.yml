- change: added
  target: request
  breaking: false
  deprecated: false
  doc_only: false
  note: Added optional num_workers to EvaluationJudgeModelConfig to control concurrent judge requests.
  paths:
    - path: components.schemas.EvaluationJudgeModelConfig.properties.num_workers
      before: 'null'
      after: '{"type":"integer","minimum":1,"example":5,"description":"Number of concurrent workers for inference requests. Overrides the default concurrency for this model. Useful for tuning throughput when using proxy endpoints (e.g. OpenRouter) or rate-limited external APIs."}'
  date: '2026-04-10'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/886
- change: added
  target: request
  breaking: false
  deprecated: false
  doc_only: false
  note: Added optional num_workers to EvaluationModelRequest to control concurrent model requests.
  paths:
    - path: components.schemas.EvaluationModelRequest.properties.num_workers
      before: 'null'
      after: '{"type":"integer","minimum":1,"example":5,"description":"Number of concurrent workers for inference requests. Overrides the default concurrency for this model. Useful for tuning throughput when using proxy endpoints (e.g. OpenRouter) or rate-limited external APIs."}'
  date: '2026-04-10'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/886
- change: added
  target: request
  breaking: false
  deprecated: false
  doc_only: false
  note: Added disable_position_bias_correction to EvaluationCompareParameters to optionally skip flipped-order judging.
  paths:
    - path: $.components.schemas.EvaluationCompareParameters.properties.disable_position_bias_correction
      before: 'null'
      after: 'type: boolean, default: false; description: When false (default), the judge runs twice per sample... When true, only the original-order pass is run...'
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/933
- change: changed
  target: request
  breaking: true
  deprecated: false
  doc_only: true
  note: EvaluationCompareParameters.input_data_file_path now documented as a Data file ID (was name).
  paths:
    - path: $.components.schemas.EvaluationCompareParameters.properties.input_data_file_path.description
      before: Data file name
      after: Data file ID
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/933
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: false
  note: EvaluationCompareParameters.model_a and model_b now inline a oneOf (EvaluationModelRequest|string) instead of referencing EvaluationModelOrString. Allowed types unchanged.
  paths:
    - path: $.components.schemas.EvaluationCompareParameters.properties.model_a
      before: '$ref: #/components/schemas/EvaluationModelOrString'
      after: 'oneOf: [ $ref: #/components/schemas/EvaluationModelRequest, { type: string, description: Column name... } ]'
    - path: $.components.schemas.EvaluationCompareParameters.properties.model_b
      before: '$ref: #/components/schemas/EvaluationModelOrString'
      after: 'oneOf: [ $ref: #/components/schemas/EvaluationModelRequest, { type: string, description: Column name... } ]'
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/933
- change: added
  target: request
  breaking: false
  deprecated: false
  doc_only: false
  note: Added max_tokens to EvaluationJudgeModelConfig to control judge output length.
  paths:
    - path: $.components.schemas.EvaluationJudgeModelConfig.properties.max_tokens
      before: 'null'
      after: 'type: integer, minimum: 1, example: 8192; description: Maximum number of tokens the judge model may generate...'
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/933
- change: added
  target: request
  breaking: false
  deprecated: false
  doc_only: false
  note: Added temperature to EvaluationJudgeModelConfig to control judge sampling; defaults to 0.05 if omitted.
  paths:
    - path: $.components.schemas.EvaluationJudgeModelConfig.properties.temperature
      before: 'null'
      after: 'type: number, minimum: 0, maximum: 2, example: 0; description: Sampling temperature for the judge model...'
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/933
- change: removed
  target: request
  breaking: false
  deprecated: false
  doc_only: false
  note: 'Removed OpenAPI format: float from numeric fields in EvaluationScoreParameters (still type number).'
  paths:
    - path: $.components.schemas.EvaluationScoreParameters.properties.max_score.format
      before: float
      after: 'null'
    - path: $.components.schemas.EvaluationScoreParameters.properties.min_score.format
      before: float
      after: 'null'
    - path: $.components.schemas.EvaluationScoreParameters.properties.pass_threshold.format
      before: float
      after: 'null'
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/933
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: true
  note: Documentation updates in EvaluationJudgeModelConfig for external API fields, model_source, and num_workers (requirements, defaults, and concurrency details).
  paths:
    - path: $.components.schemas.EvaluationJudgeModelConfig.properties.external_api_token.description
      before: Bearer/API token for external judge models.
      after: Bearer/API token for the external judge model provider. Required when model_source is 'external'.
    - path: $.components.schemas.EvaluationJudgeModelConfig.properties.external_base_url.description
      before: Base URL for external judge models. Must be OpenAI-compatible base URL.
      after: Base URL of the external inference API for the judge. Must be OpenAI-compatible. Required when model_source is 'external'.
    - path: $.components.schemas.EvaluationJudgeModelConfig.properties.model_source.description
      before: Source of the judge model.
      after: 'Source of the judge model inference: - `serverless`: Together''s shared serverless inference API... - `dedicated`: ... - `external`: ... Requires `external_api_token` and `external_base_url`... '
    - path: $.components.schemas.EvaluationJudgeModelConfig.properties.num_workers.description
      before: Number of concurrent workers for inference requests. Overrides the default concurrency for this model. Useful for tuning throughput when using proxy endpoints (e.g. OpenRouter) or rate-limited external APIs.
      after: 'Number of concurrent inference workers for the judge. Overrides the source-specific default (serverless: 25, dedicated: 5, external: 2–20). For dedicated endpoints the value is clamped to a minimum of 5 regardless of what is set here.'
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/933
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: true
  note: Documentation updates in EvaluationModelRequest for external API requirements, templating support, model_source/num_workers details, and temperature description.
  paths:
    - path: $.components.schemas.EvaluationModelRequest.properties.external_api_token.description
      before: Bearer/API token for external models.
      after: Bearer/API token for the external model provider. Required when model_source is 'external'.
    - path: $.components.schemas.EvaluationModelRequest.properties.external_base_url.description
      before: Base URL for external models. Must be OpenAI-compatible base URL
      after: Base URL of the external inference API. Must be OpenAI-compatible. Required when model_source is 'external'.
    - path: $.components.schemas.EvaluationModelRequest.properties.input_template.description
      before: Input prompt template
      after: User message template. Supports Jinja2 variables referencing dataset columns.
    - path: $.components.schemas.EvaluationModelRequest.properties.system_template.description
      before: System prompt template
      after: System prompt template. Supports Jinja2 variables referencing dataset columns.
    - path: $.components.schemas.EvaluationModelRequest.properties.model_source.description
      before: Source of the model.
      after: 'Source of the model inference: - `serverless`: Together''s shared serverless inference API... - `dedicated`: ... Authentication uses the requesting user''s Together API token automatically. - `external`: ... Requires `external_api_token` and `external_base_url`... '
    - path: $.components.schemas.EvaluationModelRequest.properties.num_workers.description
      before: Number of concurrent workers for inference requests. Overrides the default concurrency for this model. Useful for tuning throughput when using proxy endpoints (e.g. OpenRouter) or rate-limited external APIs.
      after: 'Number of concurrent inference workers. Overrides the source-specific default (serverless: 25, dedicated: 5, external: 2–20). For dedicated endpoints the value is clamped to a minimum of 5 regardless of what is set here.'
    - path: $.components.schemas.EvaluationModelRequest.properties.temperature.description
      before: Sampling temperature
      after: Sampling temperature for generation.
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/933
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: true
  note: Documentation clarified for EvaluationModelOrString string option (column name in dataset).
  paths:
    - path: $.components.schemas.EvaluationModelOrString.oneOf[0].description
      before: Field name in the input data
      after: Column name in the input dataset containing pre-generated responses
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/933
