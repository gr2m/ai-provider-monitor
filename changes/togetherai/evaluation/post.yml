- change: added
  target: request
  breaking: false
  deprecated: false
  doc_only: false
  note: Added optional num_workers to EvaluationJudgeModelConfig to control concurrent judge requests.
  paths:
    - path: components.schemas.EvaluationJudgeModelConfig.properties.num_workers
      before: 'null'
      after: '{"type":"integer","minimum":1,"example":5,"description":"Number of concurrent workers for inference requests. Overrides the default concurrency for this model. Useful for tuning throughput when using proxy endpoints (e.g. OpenRouter) or rate-limited external APIs."}'
  date: '2026-04-10'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/886
- change: added
  target: request
  breaking: false
  deprecated: false
  doc_only: false
  note: Added optional num_workers to EvaluationModelRequest to control concurrent model requests.
  paths:
    - path: components.schemas.EvaluationModelRequest.properties.num_workers
      before: 'null'
      after: '{"type":"integer","minimum":1,"example":5,"description":"Number of concurrent workers for inference requests. Overrides the default concurrency for this model. Useful for tuning throughput when using proxy endpoints (e.g. OpenRouter) or rate-limited external APIs."}'
  date: '2026-04-10'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/886
