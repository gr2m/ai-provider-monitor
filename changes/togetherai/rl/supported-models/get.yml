- change: added
  target: route
  breaking: false
  deprecated: false
  doc_only: false
  note: New GET /rl/supported-models endpoint that returns supported RL base models and their training/inference configs and limits (e.g., context length, max seq/batch sizes, LoRA limits, sampling defaults).
  paths: []
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/933
- change: removed
  target: response
  breaking: true
  deprecated: false
  doc_only: false
  note: Removed generator_config from RL.SupportedModel; per-GPU inference/training configs now live under compute_configs.
  paths:
    - path: components/schemas/RL.SupportedModel/properties/generator_config
      before: '{"$ref":"#/components/schemas/RL.ModelGeneratorConfig","description":"Inference config. Set when the model can be provisioned with generator replicas."}'
      after: 'null'
  date: '2026-08-31'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/977
- change: removed
  target: response
  breaking: true
  deprecated: false
  doc_only: false
  note: Removed trainer_config from RL.SupportedModel; training limits are now provided per GPU in compute_configs.
  paths:
    - path: components/schemas/RL.SupportedModel/properties/trainer_config
      before: '{"$ref":"#/components/schemas/RL.ModelTrainerConfig","description":"Training config. Set when the model supports at least one training mode."}'
      after: 'null'
  date: '2026-08-31'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/977
- change: added
  target: response
  breaking: false
  deprecated: false
  doc_only: false
  note: Added compute_configs array to RL.SupportedModel to expose validated GPU-specific configs.
  paths:
    - path: components/schemas/RL.SupportedModel/properties/compute_configs
      before: 'null'
      after: '{"description":"Validated GPU configurations available for this base model.","items":{"$ref":"#/components/schemas/RL.SupportedModelComputeConfig"},"type":"array"}'
  date: '2026-08-31'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/977
- change: added
  target: response
  breaking: false
  deprecated: false
  doc_only: false
  note: Added default_gpu_type to RL.SupportedModel indicating the default GPU type used when omitted.
  paths:
    - path: components/schemas/RL.SupportedModel/properties/default_gpu_type
      before: 'null'
      after: '{"description":"GPU type used when model-resource creation omits gpu_type.","enum":["H100-80GB","B200-SXM"],"example":"H100-80GB","type":"string"}'
  date: '2026-08-31'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/977
- change: changed
  target: response
  breaking: false
  deprecated: false
  doc_only: false
  note: RL.SupportedModel now requires default_gpu_type in addition to base_model.
  paths:
    - path: components/schemas/RL.SupportedModel/required
      before: '["base_model"]'
      after: '["base_model","default_gpu_type"]'
  date: '2026-08-31'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/977
- change: added
  target: response
  breaking: false
  deprecated: false
  doc_only: false
  note: Introduced RL.SupportedModelComputeConfig to describe per-GPU inference/training capabilities.
  paths:
    - path: components/schemas/RL.SupportedModelComputeConfig
      before: 'null'
      after: '{"description":"A validated hardware configuration available for an RL base model.","properties":{"generator_config":{"$ref":"#/components/schemas/RL.ModelGeneratorConfig","description":"Inference config for this GPU type. Set when the model can be provisioned with generator replicas on this GPU type."},"gpu_type":{"description":"GPU type this configuration provisions.","enum":["H100-80GB","B200-SXM"],"example":"H100-80GB","type":"string"},"trainer_config":{"$ref":"#/components/schemas/RL.ModelTrainerConfig","description":"Training config for this GPU type. Set when the model supports at least one training mode on this GPU type."}},"required":["gpu_type"],"type":"object"}'
  date: '2026-08-31'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/977
- change: changed
  target: response
  breaking: false
  deprecated: false
  doc_only: true
  note: Clarified RL.ModelGeneratorConfig description to be GPU-type specific; no schema change.
  paths:
    - path: components/schemas/RL.ModelGeneratorConfig/description
      before: Inference config. If present, the model can be provisioned with one or more generator replicas.
      after: Inference capabilities and limits for a model on one GPU type. If present, the model can be provisioned with one or more generator replicas.
  date: '2026-08-31'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/977
- change: changed
  target: response
  breaking: false
  deprecated: false
  doc_only: true
  note: Clarified RL.ModelTrainerConfig description to be GPU-type specific; no schema change.
  paths:
    - path: components/schemas/RL.ModelTrainerConfig/description
      before: Training config. Each sub-mode is present only when the model supports that training mode.
      after: Training capabilities and limits for a model on one GPU type. Each sub-mode is present only when the model supports that training mode.
  date: '2026-08-31'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/977
