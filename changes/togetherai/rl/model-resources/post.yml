- change: added
  target: route
  breaking: false
  deprecated: false
  doc_only: false
  note: Introduces POST /rl/model-resources to provision standalone model resources for RL training sessions; returns ModelResources with status, compute, and optimizer configuration.
  paths: []
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/933
- change: removed
  target: response
  breaking: true
  deprecated: false
  doc_only: false
  note: Removed enum value MODEL_RESOURCES_ERROR_CODE_CAPACITY_UNAVAILABLE from RL.ModelResourcesErrorCode used in response error.code.
  paths:
    - path: components/schemas/RL.ModelResourcesErrorCode/enum
      before: MODEL_RESOURCES_ERROR_CODE_CAPACITY_UNAVAILABLE
      after: 'null'
  date: '2026-08-25'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/946
- change: added
  target: response
  breaking: false
  deprecated: false
  doc_only: false
  note: Added enum value MODEL_RESOURCES_ERROR_CODE_CAPACITY_WAIT_TIMEOUT to RL.ModelResourcesErrorCode used in response error.code.
  paths:
    - path: components/schemas/RL.ModelResourcesErrorCode/enum
      before: 'null'
      after: MODEL_RESOURCES_ERROR_CODE_CAPACITY_WAIT_TIMEOUT
  date: '2026-08-25'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/946
- change: added
  target: request
  breaking: false
  deprecated: false
  doc_only: false
  note: Added optional gpu_type to compute_config to select GPU type when provisioning.
  paths:
    - path: components.schemas.RL.ComputeConfigCreateRequest.properties.gpu_type
      before: 'null'
      after: '{ "type": "string", "enum": ["H100-80GB", "B200-SXM"], "description": "GPU type to provision. Omit to use the model''s default GPU type." }'
  date: '2026-08-31'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/977
- change: added
  target: response
  breaking: false
  deprecated: false
  doc_only: false
  note: Added gpu_type to response compute_config to return the selected GPU type.
  paths:
    - path: components.schemas.RL.ComputeConfig.properties.gpu_type
      before: 'null'
      after: '{ "type": "string", "enum": ["H100-80GB", "B200-SXM"], "description": "GPU type selected for this resource." }'
  date: '2026-08-31'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/977
