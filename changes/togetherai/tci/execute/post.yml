- change: changed
  target: route
  breaking: false
  deprecated: false
  doc_only: true
  note: Route description wording updated; no functional change.
  paths:
    - path: /description
      before: |
        Executes the given code snippet and returns the output. Without a session_id, a new session will be created to run the code. If you do pass in a valid session_id, the code will be run in that session. This is useful for running multiple code snippets in the same environment, because dependencies and similar things are persisted
        between calls to the same session.
      after: |
        Executes the given code snippet and returns the output. Without a session_id, a new session is created to run the code. If you pass a valid session_id, the code runs in that session. This is useful for running multiple code snippets in the same environment, because dependencies and similar things are persisted
        between calls to the same session.
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/933
- change: added
  target: route
  breaking: false
  deprecated: false
  doc_only: true
  note: Added route summary.
  paths:
    - path: /summary
      before: 'null'
      after: Execute code
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/933
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: true
  note: Files property description clarified (upload timing).
  paths:
    - path: /components/schemas/ExecuteRequest/properties/files/description
      before: Files to upload to the session. If present, files will be uploaded before executing the given code.
      after: Files to upload to the session. If present, files are uploaded before executing the given code.
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/933
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: true
  note: Language description updated; removed forward-looking note about more languages.
  paths:
    - path: /components/schemas/ExecuteRequest/properties/language/description
      before: Programming language for the code to execute. Currently only supports Python, but more will be added.
      after: Programming language for the code to execute. Currently only supports Python.
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/933
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: true
  note: session_id description wording tightened (error phrasing).
  paths:
    - path: /components/schemas/ExecuteRequest/properties/session_id/description
      before: Identifier of the current session. Used to make follow-up calls. Requests will return an error if the session does not belong to the caller or has expired.
      after: Identifier of the current session. Used to make follow-up calls. Returns an error if the session does not belong to the caller or has expired.
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/933
- change: changed
  target: response
  breaking: false
  deprecated: false
  doc_only: true
  note: ExecuteResponse description wording updated (null phrasing).
  paths:
    - path: /components/schemas/ExecuteResponse/description
      before: The result of the execution. If successful, `data` contains the result and `errors` will be null. If unsuccessful, `data` will be null and `errors` will contain the errors.
      after: The result of the execution. If successful, `data` contains the result and `errors` is null. If unsuccessful, `data` is null and `errors` contains the errors.
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/933
