- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: true
  note: Updated documentation link in voice property description to use the 'supported-voices' anchor.
  paths:
    - path: components.schemas.AudioSpeechRequest.properties.voice.description
      before: |
        The voice to use for generating the audio. The voices supported are different for each model. For eg - for canopylabs/orpheus-3b-0.1-ft, one of the voices supported is tara, for hexgrad/Kokoro-82M, one of the voices supported is af_alloy and for cartesia/sonic, one of the voices supported is "friendly sidekick". <br> <br> You can view the voices supported for each model using the /v1/voices endpoint sending the model name as the query parameter. [View all supported voices here](https://docs.together.ai/docs/text-to-speech#voices-available).
      after: |
        The voice to use for generating the audio. The voices supported are different for each model. For eg - for canopylabs/orpheus-3b-0.1-ft, one of the voices supported is tara, for hexgrad/Kokoro-82M, one of the voices supported is af_alloy and for cartesia/sonic, one of the voices supported is "friendly sidekick". <br> <br> You can view the voices supported for each model using the /v1/voices endpoint sending the model name as the query parameter. [View all supported voices here](https://docs.together.ai/docs/text-to-speech#supported-voices).
  date: '2026-03-25'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/828
- change: added
  target: request
  breaking: false
  deprecated: false
  doc_only: false
  note: Added bit_rate to AudioSpeechRequest to control MP3 bitrate (only when response_format=mp3; currently on Cartesia models).
  paths:
    - path: components.schemas.AudioSpeechRequest.properties.bit_rate
      before: 'null'
      after: '{"type":"integer","default":128000,"enum":[32000,64000,96000,128000,192000],"description":"Bitrate of the MP3 audio output in bits per second. Only applicable when response_format is mp3. Higher values produce better audio quality at larger file sizes. Default is 128000. Currently supported on Cartesia models."}'
  date: '2026-03-30'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/843
- change: added
  target: request
  breaking: false
  deprecated: false
  doc_only: false
  note: Added optional extra_params object for model-specific tuning; includes pronunciation_dict for custom pronunciations.
  paths:
    - path: components/schemas/AudioSpeechRequest/properties/extra_params
      before: 'null'
      after: '{"description":"Additional model-specific parameters that fine-tune speech generation behavior.","properties":{"pronunciation_dict":{"description":"A list of pronunciation rules for specific characters or symbols. Each entry uses the format \"<source>/<replacement>\" (e.g., [\"omg/oh my god\"]) to override how the model pronounces matching tokens.","example":["omg/oh my god"],"items":{"type":"string"},"type":"array"}},"type":"object"}'
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/933
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: false
  note: Language field no longer restricted to a fixed enum; now accepts ISO 639-1 and lowercase locale codes (e.g., zh-hk).
  paths:
    - path: components/schemas/AudioSpeechRequest/properties/language/enum
      before: '["en","de","fr","es","hi","it","ja","ko","nl","pl","pt","ru","sv","tr","zh"]'
      after: 'null'
    - path: components/schemas/AudioSpeechRequest/properties/language/description
      before: Language of input text.
      after: Language or locale of input text. Accepts ISO 639-1 language codes (e.g., `en`, `fr`, `es`, `zh`) as well as locale codes for region-specific variants. Locale codes must be lowercase (e.g., `zh-hk` for Cantonese).
    - path: components/schemas/AudioSpeechRequest/properties/language/example
      before: 'null'
      after: '"en"'
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/933
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: true
  note: Updated response_encoding documentation to clarify applicability and model-specific behavior.
  paths:
    - path: components/schemas/AudioSpeechRequest/properties/response_encoding/description
      before: Audio encoding of response
      after: Audio encoding of response. Only applicable when response_format is raw or pcm. Cartesia models respect this parameter and support all values. Orpheus, Kokoro, and Minimax models always return pcm_s16le regardless of this setting.
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/933
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: true
  note: Updated sample_rate documentation to clarify model behavior (Cartesia/Minimax respect it; Orpheus/Kokoro fixed at 24000 Hz).
  paths:
    - path: components/schemas/AudioSpeechRequest/properties/sample_rate/description
      before: Sampling rate to use for the output audio. The default sampling rate for canopylabs/orpheus-3b-0.1-ft and hexgrad/Kokoro-82M is 24000 and for cartesia/sonic is 44100.
      after: Sampling rate in Hz for the output audio. Cartesia and Minimax models respect this parameter. Orpheus and Kokoro models always output at 24000 Hz regardless of this setting.
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/933
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: true
  note: Expanded voice documentation; added Kokoro voice mixing syntax with optional weights.
  paths:
    - path: components/schemas/AudioSpeechRequest/properties/voice/description
      before: |
        The voice to use for generating the audio. The voices supported are different for each model. For eg - for canopylabs/orpheus-3b-0.1-ft, one of the voices supported is tara, for hexgrad/Kokoro-82M, one of the voices supported is af_alloy and for cartesia/sonic, one of the voices supported is "friendly sidekick". <br> <br> You can view the voices supported for each model using the /v1/voices endpoint sending the model name as the query parameter. [View all supported voices here](https://docs.together.ai/docs/text-to-speech#supported-voices).
      after: |
        The voice to use for generating the audio. The voices supported are different for each model. For eg - for canopylabs/orpheus-3b-0.1-ft, one of the voices supported is tara, for hexgrad/Kokoro-82M, one of the voices supported is af_alloy and for cartesia/sonic, one of the voices supported is "friendly sidekick". <br> <br> You can view the voices supported for each model using the /v1/voices endpoint sending the model name as the query parameter. [View all supported voices here](https://docs.together.ai/docs/text-to-speech#supported-voices). <br> <br> `hexgrad/Kokoro-82M` additionally supports voice mixing, where two or more voices are combined into a single blended voice by joining their names with `+` (e.g. `af_bella+af_heart`). Optional per-voice weights can be provided in parentheses (e.g. `af_bella(2)+af_heart(1)`). Other models require a single voice name.
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/933
