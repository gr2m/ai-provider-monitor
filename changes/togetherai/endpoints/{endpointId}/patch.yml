- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: true
  note: Updated description for request field inactive_timeout to clarify automatic stop wording.
  paths:
    - path: requestBody/content/application/json/schema/properties/inactive_timeout/description
      before: The number of minutes of inactivity after which the endpoint will be automatically stopped. Set to 0 to disable automatic timeout.
      after: The number of minutes of inactivity after which the endpoint stops automatically. Set to 0 to disable automatic timeout.
  date: '2026-08-24'
- change: removed
  target: request
  breaking: false
  deprecated: false
  doc_only: false
  note: Removed int32 format from Autoscaling min_replicas and max_replicas in request schema (still type integer).
  paths:
    - path: components/schemas/Autoscaling/properties/max_replicas/format
      before: int32
      after: 'null'
    - path: components/schemas/Autoscaling/properties/min_replicas/format
      before: int32
      after: 'null'
  date: '2026-08-24'
- change: removed
  target: response
  breaking: false
  deprecated: false
  doc_only: false
  note: Removed int32 format from Autoscaling min_replicas and max_replicas in response schema (still type integer).
  paths:
    - path: components/schemas/Autoscaling/properties/max_replicas/format
      before: int32
      after: 'null'
    - path: components/schemas/Autoscaling/properties/min_replicas/format
      before: int32
      after: 'null'
  date: '2026-08-24'
