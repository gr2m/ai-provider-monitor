- change: changed
  target: route
  breaking: false
  deprecated: false
  doc_only: true
  note: Route description and summary wording updated; no functional changes.
  paths:
    - path: description
      before: Creates a new dedicated endpoint for serving models. The endpoint will automatically start after creation. You can deploy any supported model on hardware configurations that meet the model's requirements.
      after: Creates a new dedicated endpoint for serving models. The endpoint starts automatically after creation. You can deploy any supported model on hardware configurations that meet the model's requirements.
    - path: summary
      before: Create a dedicated endpoint, it will start automatically
      after: Create a dedicated endpoint
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/933
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: true
  note: Clarified wording for CreateEndpointRequest.inactive_timeout; behavior unchanged.
  paths:
    - path: components/schemas/CreateEndpointRequest/properties/inactive_timeout/description
      before: The number of minutes of inactivity after which the endpoint will be automatically stopped. Set to null, omit or set to 0 to disable automatic timeout.
      after: The number of minutes of inactivity after which the endpoint stops automatically. Set to null, omit, or set to 0 to disable automatic timeout.
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/933
- change: removed
  target: request
  breaking: false
  deprecated: false
  doc_only: false
  note: Removed int32 format from Autoscaling min_replicas and max_replicas in request schema.
  paths:
    - path: components/schemas/Autoscaling/properties/max_replicas/format
      before: int32
      after: 'null'
    - path: components/schemas/Autoscaling/properties/min_replicas/format
      before: int32
      after: 'null'
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/933
- change: removed
  target: response
  breaking: false
  deprecated: false
  doc_only: false
  note: Removed int32 format from Autoscaling min_replicas and max_replicas in response schema.
  paths:
    - path: components/schemas/Autoscaling/properties/max_replicas/format
      before: int32
      after: 'null'
    - path: components/schemas/Autoscaling/properties/min_replicas/format
      before: int32
      after: 'null'
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/933
