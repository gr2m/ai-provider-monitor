- change: added
  target: request
  breaking: false
  deprecated: false
  doc_only: false
  note: Added optional 'version' field to VolumeMount used in request volumes
  paths:
    - path: components.schemas.VolumeMount.properties.version
      before: 'null'
      after: '{"type":"integer","description":"Version is the volume version to mount. On create, defaults to the latest version. On update, defaults to the currently mounted version."}'
  date: '2026-03-02'
  diff_url: https://github.com/gr2m/ai-provider-api-changes/pull/731
- change: added
  target: response
  breaking: false
  deprecated: false
  doc_only: false
  note: Added optional 'version' field to VolumeMount returned in response volumes
  paths:
    - path: components.schemas.VolumeMount.properties.version
      before: 'null'
      after: '{"type":"integer","description":"Version is the volume version to mount. On create, defaults to the latest version. On update, defaults to the currently mounted version."}'
  date: '2026-03-02'
  diff_url: https://github.com/gr2m/ai-provider-api-changes/pull/731
- change: changed
  target: request
  breaking: true
  deprecated: false
  doc_only: false
  note: CreateDeploymentRequest.autoscaling changed from free-form string map to a typed oneOf (HTTPAutoscalingConfig | QueueAutoscalingConfig | CustomMetricAutoscalingConfig).
  paths:
    - path: components/schemas/CreateDeploymentRequest/properties/autoscaling
      before: '{"type":"object","additionalProperties":{"type":"string"},"description":"Autoscaling configuration as key-value pairs. Example: {\"metric\": \"QueueBacklogPerWorker\", \"target\": \"10\"} to scale based on queue backlog"}'
      after: '{"oneOf":[{"$ref":"#/components/schemas/HTTPAutoscalingConfig"},{"$ref":"#/components/schemas/QueueAutoscalingConfig"},{"$ref":"#/components/schemas/CustomMetricAutoscalingConfig"}],"description":"Autoscaling configuration. Example: {\"metric\": \"QueueBacklogPerWorker\", \"target\": 1.01} to scale based on queue backlog. Omit or set to null to disable autoscaling"}'
  date: '2026-03-10'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/761
- change: changed
  target: response
  breaking: true
  deprecated: false
  doc_only: false
  note: DeploymentResponseItem.autoscaling changed from free-form string map to a typed oneOf (HTTPAutoscalingConfig | QueueAutoscalingConfig | CustomMetricAutoscalingConfig).
  paths:
    - path: components/schemas/DeploymentResponseItem/properties/autoscaling
      before: '{"type":"object","additionalProperties":{"type":"string"},"description":"Autoscaling contains autoscaling configuration parameters for this deployment"}'
      after: '{"oneOf":[{"$ref":"#/components/schemas/HTTPAutoscalingConfig"},{"$ref":"#/components/schemas/QueueAutoscalingConfig"},{"$ref":"#/components/schemas/CustomMetricAutoscalingConfig"}],"description":"Autoscaling contains autoscaling configuration parameters for this deployment. Omitted when autoscaling is disabled (nil)"}'
  date: '2026-03-10'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/761
- change: added
  target: route
  breaking: false
  deprecated: false
  doc_only: false
  note: Added HTTPAutoscalingConfig schema for HTTP-based autoscaling metrics.
  paths:
    - path: components/schemas/HTTPAutoscalingConfig
      before: 'null'
      after: '{"description":"Autoscaling config for HTTPTotalRequests and HTTPAvgRequestDuration metrics","type":"object","properties":{"metric":{"type":"string","enum":["HTTPTotalRequests","HTTPAvgRequestDuration"],"description":"Metric must be HTTPTotalRequests or HTTPAvgRequestDuration","example":"HTTPTotalRequests"},"target":{"type":"number","description":"Target is the threshold value. Default: 100 for HTTPTotalRequests, 500 (ms) for HTTPAvgRequestDuration","example":100},"time_interval_minutes":{"type":"integer","description":"TimeIntervalMinutes is the rate window in minutes. Default: 10","example":10}}}'
  date: '2026-03-10'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/761
- change: added
  target: route
  breaking: false
  deprecated: false
  doc_only: false
  note: Added QueueAutoscalingConfig schema for queue backlog autoscaling.
  paths:
    - path: components/schemas/QueueAutoscalingConfig
      before: 'null'
      after: '{"description":"Autoscaling config for QueueBacklogPerWorker metric","type":"object","properties":{"metric":{"type":"string","enum":["QueueBacklogPerWorker"],"description":"Metric must be QueueBacklogPerWorker","example":"QueueBacklogPerWorker"},"model":{"type":"string","description":"Model overrides the model name for queue status lookup. Defaults to the deployment app name"},"target":{"type":"number","description":"Target is the threshold value. Default: 1.01","example":1.01}}}'
  date: '2026-03-10'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/761
- change: added
  target: route
  breaking: false
  deprecated: false
  doc_only: false
  note: Added CustomMetricAutoscalingConfig schema for custom Prometheus metric autoscaling.
  paths:
    - path: components/schemas/CustomMetricAutoscalingConfig
      before: 'null'
      after: '{"description":"Autoscaling config for CustomMetric metric","type":"object","properties":{"metric":{"type":"string","enum":["CustomMetric"],"description":"Metric must be CustomMetric","example":"CustomMetric"},"custom_metric_name":{"type":"string","description":"CustomMetricName is the Prometheus metric name. Required. Must match [a-zA-Z_:][a-zA-Z0-9_:]*","example":"my_custom_metric"},"target":{"type":"number","description":"Target is the threshold value. Default: 500","example":500}}}'
  date: '2026-03-10'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/761
