- change: added
  target: route
  breaking: false
  deprecated: false
  doc_only: false
  note: Introduces GET /fine-tunes/models/limits to retrieve fine-tuning limits (epochs, learning rates, batch sizes, LoRA settings and capabilities) for a specified model via the required model_name query; returns FineTuneModelLimits on success or 404 if the model is unsupported.
  paths: []
  date: '2026-03-06'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/753
- change: added
  target: response
  breaking: false
  deprecated: false
  doc_only: false
  note: Added default_gradient_accumulation_steps (integer) to model limits.
  paths:
    - path: components/schemas/FineTuneModelLimits/properties/default_gradient_accumulation_steps
      before: 'null'
      after: '{"description":"Default gradient accumulation steps used when a fine-tune request omits the value or sets it to 0.","type":"integer"}'
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/933
- change: added
  target: response
  breaking: false
  deprecated: false
  doc_only: false
  note: Added max_num_checkpoints (integer) to model limits.
  paths:
    - path: components/schemas/FineTuneModelLimits/properties/max_num_checkpoints
      before: 'null'
      after: '{"description":"Maximum number of checkpoints that can be saved during a fine-tuning job.","type":"integer"}'
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/933
- change: added
  target: response
  breaking: false
  deprecated: false
  doc_only: false
  note: Added max_seq_length_dpo (integer) to model limits.
  paths:
    - path: components/schemas/FineTuneModelLimits/properties/max_seq_length_dpo
      before: 'null'
      after: '{"description":"Maximum sequence length supported for DPO training.","type":"integer"}'
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/933
- change: added
  target: response
  breaking: false
  deprecated: false
  doc_only: false
  note: Added max_seq_length_sft (integer) to model limits.
  paths:
    - path: components/schemas/FineTuneModelLimits/properties/max_seq_length_sft
      before: 'null'
      after: '{"description":"Maximum sequence length supported for SFT training.","type":"integer"}'
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/933
- change: added
  target: response
  breaking: false
  deprecated: false
  doc_only: false
  note: Added min_max_seq_length (integer) to model limits.
  paths:
    - path: components/schemas/FineTuneModelLimits/properties/min_max_seq_length
      before: 'null'
      after: '{"description":"Minimum value allowed for the max_seq_length hyperparameter.","type":"integer"}'
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/933
- change: added
  target: response
  breaking: false
  deprecated: false
  doc_only: false
  note: Added supports_full_training (boolean) to model limits.
  paths:
    - path: components/schemas/FineTuneModelLimits/properties/supports_full_training
      before: 'null'
      after: '{"description":"Whether the model supports full (non-LoRA) fine-tuning. When false, only LoRA fine-tuning is available and the full_training limits are reported as zero.","type":"boolean"}'
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/933
- change: changed
  target: response
  breaking: false
  deprecated: false
  doc_only: true
  note: Clarified description of merge_output_lora without changing its type or behavior.
  paths:
    - path: components/schemas/FineTuneModelLimits/properties/merge_output_lora/description
      before: '"Whether to merge the output LoRA."'
      after: '"Whether a merged checkpoint (the base model with the trained LoRA adapter fused in) is produced for LoRA fine-tunes of this model, in addition to the standalone adapter."'
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/933
- change: changed
  target: response
  breaking: false
  deprecated: false
  doc_only: false
  note: Updated required fields to include lora_training, supports_full_training, default_gradient_accumulation_steps, max_num_checkpoints, max_seq_length_sft, max_seq_length_dpo, and min_max_seq_length.
  paths:
    - path: components/schemas/FineTuneModelLimits/required
      before: '["model_name","max_num_epochs","max_num_evals","max_learning_rate","min_learning_rate","supports_vision","supports_tools","supports_reasoning","merge_output_lora"]'
      after: '["model_name","lora_training","max_num_epochs","max_num_evals","max_num_checkpoints","max_learning_rate","min_learning_rate","supports_full_training","supports_vision","supports_tools","supports_reasoning","merge_output_lora","min_max_seq_length","max_seq_length_sft","max_seq_length_dpo","default_gradient_accumulation_steps"]'
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/933
