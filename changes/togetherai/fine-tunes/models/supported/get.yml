- change: added
  target: route
  breaking: false
  deprecated: false
  doc_only: false
  note: Introduces GET /fine-tunes/models/supported to list all fine-tuning-supported models or check support for a specific model via the model_name query. Returns either a models array or a supported boolean.
  paths: []
  date: '2026-03-06'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/753
- change: removed
  target: request
  breaking: true
  deprecated: false
  doc_only: false
  note: Removed query parameter 'model_name'; endpoint no longer accepts model-specific checks.
  paths:
    - path: parameters[0]
      before: '{"in":"query","name":"model_name","required":false,"schema":{"description":"Optional model name to check support for. If omitted, returns all supported models.","type":"string"}}'
      after: 'null'
  date: '2026-03-06'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/754
- change: removed
  target: response
  breaking: true
  deprecated: false
  doc_only: false
  note: 'Removed response variant returning { supported: boolean } for model-specific checks.'
  paths:
    - path: responses.200.content.application/json.schema.oneOf[1]
      before: '{"properties":{"supported":{"description":"Whether the specified model is supported.","type":"boolean"}},"required":["supported"],"type":"object"}'
      after: 'null'
  date: '2026-03-06'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/754
- change: changed
  target: response
  breaking: true
  deprecated: false
  doc_only: false
  note: 'Response schema changed from oneOf to a single object with { models: string[] }.'
  paths:
    - path: responses.200.content.application/json.schema
      before: '{"oneOf":[{"properties":{"models":{"description":"List of supported model names.","items":{"type":"string"},"type":"array"}},"required":["models"],"type":"object"},{"properties":{"supported":{"description":"Whether the specified model is supported.","type":"boolean"}},"required":["supported"],"type":"object"}]}'
      after: '{"properties":{"models":{"description":"List of supported model names.","items":{"type":"string"},"type":"array"}},"required":["models"],"type":"object"}'
  date: '2026-03-06'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/754
- change: changed
  target: route
  breaking: false
  deprecated: false
  doc_only: true
  note: Route description updated to remove mention of checking a specific model.
  paths:
    - path: description
      before: List models supported for fine-tuning, or check if a specific model is supported.
      after: List models supported for fine-tuning.
  date: '2026-03-06'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/754
- change: changed
  target: response
  breaking: false
  deprecated: false
  doc_only: true
  note: 200 response description updated to reflect only listing models.
  paths:
    - path: responses.200.description
      before: Supported models or support status for a specific model.
      after: List of supported models.
  date: '2026-03-06'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/754
