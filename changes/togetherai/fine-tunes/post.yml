- change: added
  target: request
  breaking: false
  deprecated: false
  doc_only: false
  note: Added default=null to training_type; omitting it now defaults the job to LoRA.
  paths:
    - path: requestBody.content.application/json.schema.properties.training_type.default
      before: 'null'
      after: 'null'
  date: '2026-03-04'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/741
- change: added
  target: request
  breaking: false
  deprecated: false
  doc_only: true
  note: Added description for training_type clarifying LoRA is the default when omitted.
  paths:
    - path: requestBody.content.application/json.schema.properties.training_type.description
      before: 'null'
      after: The training type to use. If not provided, the job will default to LoRA training type.
  date: '2026-03-04'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/741
- change: added
  target: request
  breaking: false
  deprecated: false
  doc_only: false
  note: Added wandb_entity field to specify the Weights & Biases entity for the run.
  paths:
    - path: requestBody.content.application/json.schema.properties.wandb_entity
      before: 'null'
      after: '{"description":"The Weights & Biases entity for your run.","type":"string"}'
  date: '2026-03-04'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/744
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: false
  note: training_type union constraint changed from oneOf to anyOf (more permissive validation)
  paths:
    - path: $.requestBody.content.application/json.schema.properties.training_type.oneOf
      before: '[{"$ref":"#/components/schemas/FullTrainingType"},{"$ref":"#/components/schemas/LoRATrainingType"}]'
      after: 'null'
    - path: $.requestBody.content.application/json.schema.properties.training_type.anyOf
      before: 'null'
      after: '[{"$ref":"#/components/schemas/FullTrainingType"},{"$ref":"#/components/schemas/LoRATrainingType"}]'
  date: '2026-03-04'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/745
- change: added
  target: request
  breaking: false
  deprecated: false
  doc_only: false
  note: 'training_type now explicitly allows null (nullable: true)'
  paths:
    - path: $.requestBody.content.application/json.schema.properties.training_type.nullable
      before: 'null'
      after: 'true'
  date: '2026-03-04'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/745
- change: added
  target: request
  breaking: false
  deprecated: false
  doc_only: false
  note: Added optional random_seed parameter to control training determinism.
  paths:
    - path: requestBody.content.application/json.schema.properties.random_seed
      before: 'null'
      after: '{"type":"integer","nullable":true,"description":"Random seed for reproducible training. When set, the same seed produces the same run (e.g. data shuffle, init). If omitted or null, the server applies its default seed (e.g. 42).\n"}'
  date: '2026-03-19'
- change: added
  target: response
  breaking: false
  deprecated: false
  doc_only: false
  note: Added random_seed field in FinetuneResponseTruncated to report the seed used.
  paths:
    - path: components.schemas.FinetuneResponseTruncated.properties.random_seed
      before: 'null'
      after: '{"type":"integer","nullable":true,"description":"Random seed used for training. Integer when set; null if not stored (e.g. legacy jobs) or no explicit seed was recorded.\n"}'
  date: '2026-03-19'
