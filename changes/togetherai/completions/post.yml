- change: changed
  target: response
  breaking: true
  deprecated: false
  doc_only: false
  note: '''prompt'' field in CompletionResponse is now required'
  paths:
    - path: components/schemas/CompletionResponse/required
      before: '["id","choices","usage","created","model","object"]'
      after: '["id","choices","usage","created","prompt","model","object"]'
  date: '2026-03-03'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/737
- change: changed
  target: response
  breaking: false
  deprecated: false
  doc_only: true
  note: Added descriptive text to CompletionResponse.prompt about echo/logprobs behavior
  paths:
    - path: components/schemas/CompletionResponse/properties/prompt/description
      before: 'null'
      after: When `echo` is true, the prompt is included in the response. Additionally, when `logprobs` is also provided, log probability information is provided on the prompt.
  date: '2026-03-03'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/737
- change: added
  target: response
  breaking: false
  deprecated: false
  doc_only: false
  note: Added top_logprobs to LogprobsPart to return top token log probabilities
  paths:
    - path: components/schemas/LogprobsPart/properties/top_logprobs
      before: 'null'
      after: '{"$ref":"#/components/schemas/TopLogprobs"}'
  date: '2026-03-03'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/737
- change: added
  target: response
  breaking: false
  deprecated: false
  doc_only: false
  note: Introduced TopLogprobs schema (map of token to logprob)
  paths:
    - path: components/schemas/TopLogprobs
      before: 'null'
      after: '{"additionalProperties":{"type":"number"},"description":"Top log probabilities for the tokens.","type":"object"}'
  date: '2026-03-03'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/737
- change: added
  target: response
  breaking: false
  deprecated: false
  doc_only: false
  note: Added new field reasoning_content to streaming delta objects in CompletionChoice.
  paths:
    - path: components.schemas.CompletionChoice.properties.delta.properties.reasoning_content
      before: 'null'
      after: '{"type":"string","nullable":true}'
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/933
- change: removed
  target: request
  breaking: false
  deprecated: false
  doc_only: false
  note: Removed OpenAPI format hints from several numeric fields in CompletionRequest.
  paths:
    - path: components.schemas.CompletionRequest.properties.frequency_penalty.format
      before: float
      after: 'null'
    - path: components.schemas.CompletionRequest.properties.logit_bias.additionalProperties.format
      before: float
      after: 'null'
    - path: components.schemas.CompletionRequest.properties.min_p.format
      before: float
      after: 'null'
    - path: components.schemas.CompletionRequest.properties.presence_penalty.format
      before: float
      after: 'null'
    - path: components.schemas.CompletionRequest.properties.repetition_penalty.format
      before: float
      after: 'null'
    - path: components.schemas.CompletionRequest.properties.temperature.format
      before: float
      after: 'null'
    - path: components.schemas.CompletionRequest.properties.top_k.format
      before: int32
      after: 'null'
    - path: components.schemas.CompletionRequest.properties.top_p.format
      before: float
      after: 'null'
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/933
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: true
  note: Minor wording updates to parameter descriptions (echo, logprobs, stop).
  paths:
    - path: components.schemas.CompletionRequest.properties.echo.description
      before: If true, the response will contain the prompt. Can be used with `logprobs` to return prompt logprobs.
      after: If true, the response contains the prompt. Can be used with `logprobs` to return prompt logprobs.
    - path: components.schemas.CompletionRequest.properties.logprobs.description
      before: An integer between 0 and 20 of the top k tokens to return log probabilities for at each generation step, instead of just the sampled token. Log probabilities help assess model confidence in token predictions.
      after: An integer between 0 and 20 of the top k tokens to return log probabilities for at each generation step, instead of only the sampled token. Log probabilities help assess model confidence in token predictions.
    - path: components.schemas.CompletionRequest.properties.stop.description
      before: A list of string sequences that will truncate (stop) inference text output. For example, "</s>" will stop generation as soon as the model generates the given token.
      after: A list of string sequences that truncate (stop) inference text output. For example, "</s>" stops generation as soon as the model generates the given token.
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/933
