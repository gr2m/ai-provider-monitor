- change: added
  target: route
  breaking: false
  deprecated: false
  doc_only: false
  note: Adds POST /projects/{projectId}/endpoints/{endpointId}/rollouts to create a rollout in the PENDING state without shifting traffic. Supports blue-green, canary (with optional metric gates), and rolling strategies between sourceDeploymentId and targetDeploymentId; returns a Rollout and must be started separately.
  paths: []
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/933
- change: removed
  target: request
  breaking: true
  deprecated: false
  doc_only: false
  note: '''serving_latency'' was removed from DE.MetricRule.name enum; requests using it will now be rejected.'
  paths:
    - path: components/schemas/DE.MetricRule/properties/name/enum
      before: '["inflight_requests","router_error_rate","router_latency","serving_latency"]'
      after: '["inflight_requests","router_error_rate","router_latency"]'
  date: '2026-08-25'
- change: added
  target: request
  breaking: false
  deprecated: false
  doc_only: true
  note: Added description to DE.MetricRule.name noting that `serving_latency` is retired.
  paths:
    - path: components/schemas/DE.MetricRule/properties/name/description
      before: 'null'
      after: Required catalogue key for the metric to gate on. `serving_latency` is retired.
  date: '2026-08-25'
