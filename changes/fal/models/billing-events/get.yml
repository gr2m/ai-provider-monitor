- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: true
  note: Clarified that the 'end' query parameter is exclusive and updated its example; no schema changes.
  paths:
    - path: parameters[3].description
      before: End date in ISO8601 format (e.g., '2025-01-31T23:59:59Z' or '2025-01-31'). Defaults to current time.
      after: End date in ISO8601 format, exclusive (e.g., '2025-02-01T00:00:00Z' or '2025-02-01'). Data up to but not including this timestamp is returned. Defaults to current time.
    - path: parameters[3].schema.description
      before: End date in ISO8601 format (e.g., '2025-01-31T23:59:59Z' or '2025-01-31'). Defaults to current time.
      after: End date in ISO8601 format, exclusive (e.g., '2025-02-01T00:00:00Z' or '2025-02-01'). Data up to but not including this timestamp is returned. Defaults to current time.
    - path: parameters[3].schema.example
      before: '2025-01-31T23:59:59Z'
      after: '2025-02-01T00:00:00Z'
  date: '2026-03-16'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/787
- change: added
  target: request
  breaking: false
  deprecated: false
  doc_only: false
  note: Added optional api_key_id query filter to filter events by API key ID(s).
  paths:
    - path: parameters[name=api_key_id]
      before: 'null'
      after: '{"description":"Filter by specific API key ID(s). Accepts 1-50 key IDs. Supports comma-separated values: ?api_key_id=key1,key2 or array syntax: ?api_key_id=key1&api_key_id=key2","explode":true,"in":"query","name":"api_key_id","required":false,"schema":{"anyOf":[{"type":"string"},{"items":{"type":"string"},"type":"array"}],"description":"Filter by specific API key ID(s). Accepts 1-50 key IDs. Supports comma-separated values: ?api_key_id=key1,key2 or array syntax: ?api_key_id=key1&api_key_id=key2","example":["abc123"]},"style":"form"}'
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/925
- change: added
  target: request
  breaking: false
  deprecated: false
  doc_only: false
  note: Added optional login_username query filter to filter events by team member login username(s).
  paths:
    - path: parameters[name=login_username]
      before: 'null'
      after: '{"description":"Filter by team member login username(s) (nickname). Accepts 1-50 usernames. Supports comma-separated values: ?login_username=alice,bob or array syntax: ?login_username=alice&login_username=bob","explode":true,"in":"query","name":"login_username","required":false,"schema":{"anyOf":[{"type":"string"},{"items":{"type":"string"},"type":"array"}],"description":"Filter by team member login username(s) (nickname). Accepts 1-50 usernames. Supports comma-separated values: ?login_username=alice,bob or array syntax: ?login_username=alice&login_username=bob","example":["alice"]},"style":"form"}'
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/925
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: true
  note: Expanded documentation for 'expand' parameter to include 'auth_method_structured' option and clarify labels.
  paths:
    - path: parameters[name=expand]/schema/description
      before: Data to include in the response. Use 'auth_method' to include authentication method information (formatted with user key aliases).
      after: Data to include in the response. Use 'auth_method' for a formatted authentication method label, and 'auth_method_structured' for a machine-readable auth method object (detail, api_key_id, login_username).
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/925
- change: added
  target: response
  breaking: false
  deprecated: false
  doc_only: false
  note: Added structured authentication info 'auth_method_structured' in billing event items (optional via expand).
  paths:
    - path: responses/200/content/application/json/schema/properties/billing_events/items/properties/auth_method_structured
      before: 'null'
      after: '{"description":"Structured authentication method identity. Only populated when ''auth_method_structured'' is included in expand parameter.","properties":{"api_key_id":{"description":"API key ID, present when the request was made with an API key.","type":"string"},"detail":{"description":"Human-readable summary of the authentication method (key alias, login username, or ''User token''/''None'').","type":"string"},"login_username":{"description":"Team member login username (nickname), present when the request was made with a user login token resolvable to a team member.","type":"string"}},"required":["detail"],"type":"object"}'
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/925
- change: added
  target: response
  breaking: false
  deprecated: false
  doc_only: false
  note: 'Added per-request USD cost breakdown fields: cost_subtotal, cost_discount, and cost_total.'
  paths:
    - path: responses/200/content/application/json/schema/properties/billing_events/items/properties/cost_subtotal
      before: 'null'
      after: '{"description":"Cost before discounts in USD (output_units × unit_price)","minimum":0,"type":"number"}'
    - path: responses/200/content/application/json/schema/properties/billing_events/items/properties/cost_discount
      before: 'null'
      after: '{"description":"Discount applied to this request in USD (cost_subtotal − cost_total)","minimum":0,"type":"number"}'
    - path: responses/200/content/application/json/schema/properties/billing_events/items/properties/cost_total
      before: 'null'
      after: '{"description":"Amount charged after discounts in USD (cost_subtotal − cost_discount)","minimum":0,"type":"number"}'
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/925
- change: changed
  target: response
  breaking: false
  deprecated: false
  doc_only: false
  note: Billing event item 'required' list updated to include cost_subtotal, cost_discount, and cost_total.
  paths:
    - path: responses/200/content/application/json/schema/properties/billing_events/items/required
      before: '["request_id","endpoint_id","timestamp","output_units","unit_price","percent_discount","cost_estimate_nano_usd"]'
      after: '["request_id","endpoint_id","timestamp","output_units","unit_price","percent_discount","cost_subtotal","cost_discount","cost_total","cost_estimate_nano_usd"]'
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/925
- change: changed
  target: response
  breaking: false
  deprecated: false
  doc_only: true
  note: Clarified cost_estimate_nano_usd description to equal cost_total in nano USD.
  paths:
    - path: responses/200/content/application/json/schema/properties/billing_events/items/properties/cost_estimate_nano_usd/description
      before: Estimated cost in nano USD (1 USD = 1,000,000,000 nano USD)
      after: Amount charged after discounts in nano USD — the same charge as cost_total (1 USD = 1,000,000,000 nano USD)
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/925
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: true
  note: 'Operation description updated: added per-request cost breakdown, increased page limit to 10000, added 90-day date cap, and updated docs link.'
  paths:
    - path: description
      before: |-

        Returns paginated individual billing event records with filters
        for endpoint and date range. Each record includes the request ID, timestamp,
        endpoint, and output units billed.

        **Key Features:**
        - Individual billing event records for each API request
        - Flexible date range filtering
        - Optional endpoint filtering
        - Cursor-based pagination for efficient large dataset queries
        - Limited to 100 records per page for performance

        **Common Use Cases:**
        - Audit individual billing events
        - Track request patterns and volumes
        - Debug specific requests by ID
        - Monitor billing unit consumption per request

        See [fal.ai docs](https://docs.fal.ai/model-apis/faq) for more details.
            
      after: |-

        Returns paginated individual billing event records with filters
        for endpoint and date range. Each record includes the request ID, timestamp,
        endpoint, output units billed, and a cost breakdown in USD (cost_subtotal,
        cost_discount, cost_total; cost_estimate_nano_usd carries cost_total in nano USD).

        **Key Features:**
        - Individual billing event records for each API request
        - Per-request cost breakdown before and after discounts
        - Flexible date range filtering
        - Optional endpoint filtering
        - Cursor-based pagination for efficient large dataset queries
        - Limited to 10000 records per page for performance
        - Date range capped at 90 days per request

        **Common Use Cases:**
        - Audit individual billing events
        - Track request patterns and volumes
        - Debug specific requests by ID
        - Monitor billing unit consumption per request

        See [fal.ai docs](https://fal.ai/docs/documentation/model-apis/faq) for more details.
            
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/925
- change: added
  target: response
  breaking: false
  deprecated: false
  doc_only: false
  note: Added quantity field to billing_events items (billable units consumed).
  paths:
    - path: responses.200.content.application/json.schema.properties.billing_events.items.properties.quantity
      before: 'null'
      after: '{"description":"Billable units consumed, in the unit named by `unit`. Same value as the deprecated `output_units`.","minimum":0,"type":["number","null"]}'
  date: '2026-09-15'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/1027
- change: added
  target: response
  breaking: false
  deprecated: false
  doc_only: false
  note: Added unit field to billing_events items (name of the billing unit).
  paths:
    - path: responses.200.content.application/json.schema.properties.billing_events.items.properties.unit
      before: 'null'
      after: '{"description":"The billing unit these units are counted in (e.g. ''image'', ''second'', ''megapixel''). Null when the reporter recorded none.","type":["string","null"]}'
  date: '2026-09-15'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/1027
- change: changed
  target: response
  breaking: false
  deprecated: true
  doc_only: false
  note: Marked output_units as deprecated in favor of quantity and updated its description.
  paths:
    - path: responses.200.content.application/json.schema.properties.billing_events.items.properties.output_units
      before: '{"description":"Custom billing units for this request","minimum":0,"type":["number","null"]}'
      after: '{"deprecated":true,"description":"Deprecated: use quantity. Same value as quantity.","minimum":0,"type":["number","null"]}'
  date: '2026-09-15'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/1027
- change: changed
  target: response
  breaking: false
  deprecated: false
  doc_only: true
  note: Updated description of cost_estimate_nano_usd to clarify precision and purpose.
  paths:
    - path: responses.200.content.application/json.schema.properties.billing_events.items.properties.cost_estimate_nano_usd.description
      before: Amount charged after discounts in nano USD — the same charge as cost_total (1 USD = 1,000,000,000 nano USD)
      after: 'Amount charged after discounts in nano USD. The precision-preserving representation of cost_total, not a legacy duplicate: an integer nano-USD amount avoids the float error that accumulates when summing sub-cent charges (1 USD = 1,000,000,000 nano USD).'
  date: '2026-09-15'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/1027
- change: changed
  target: response
  breaking: false
  deprecated: false
  doc_only: true
  note: Clarified description of unit_price to explain list vs negotiated rate context.
  paths:
    - path: responses.200.content.application/json.schema.properties.billing_events.items.properties.unit_price.description
      before: Unit price for this request
      after: 'Per-unit price this line is measured against: the list rate when a discount is reported in percent_discount, otherwise the negotiated rate after any price override and before percentage discounts.'
  date: '2026-09-15'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/1027
- change: changed
  target: response
  breaking: false
  deprecated: false
  doc_only: false
  note: Updated required fields for billing_events items to include quantity and unit.
  paths:
    - path: responses.200.content.application/json.schema.properties.billing_events.items.required
      before: '["request_id","endpoint_id","timestamp","output_units","unit_price","percent_discount","cost_subtotal","cost_discount","cost_total","cost_estimate_nano_usd"]'
      after: '["request_id","endpoint_id","timestamp","quantity","output_units","unit","unit_price","percent_discount","cost_subtotal","cost_discount","cost_total","cost_estimate_nano_usd"]'
  date: '2026-09-15'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/1027
- change: added
  target: request
  breaking: false
  deprecated: false
  doc_only: false
  note: Added 'source' query parameter to choose data feed ('billed' or 'tagged-billed'); defaults to 'billed'.
  paths:
    - path: parameters[name=source]
      before: 'null'
      after: '{"in":"query","name":"source","required":false,"description":"Data source. ''billed'' is the default billed-event feed. ''tagged-billed'' reads the tagged billing-event pivot instead, populating each row''s ''tags'' and enabling the ''tag'' filter; it requires tagged reporting to be enabled for the account, and recent events are delayed relative to ''billed''.","schema":{"type":"string","enum":["billed","tagged-billed"],"default":"billed","example":"billed"}}'
  date: '2026-09-23'
- change: added
  target: request
  breaking: false
  deprecated: false
  doc_only: false
  note: Added 'tag' query parameter to filter results by X-Fal-Tags key=value pairs (AND across different keys; OR across repeated values for the same key).
  paths:
    - path: parameters[name=tag]
      before: 'null'
      after: '{"in":"query","name":"tag","required":false,"explode":true,"style":"form","description":"Filter by X-Fal-Tags ''key=value'' pairs. Accepts 1-10 filters: different keys are AND-combined (?tag=env=prod&tag=team=design), and repeating a key matches any of its values. Use the value ''(untagged)'' to match requests that did not set the key.","schema":{"anyOf":[{"type":"string"},{"type":"array","items":{"type":"string"}}],"example":["env=prod"]}}'
  date: '2026-09-23'
- change: added
  target: response
  breaking: false
  deprecated: false
  doc_only: false
  note: Added 'tags' object on each billing event, populated when source=tagged-billed, containing the request's activated X-Fal-Tags.
  paths:
    - path: responses.200.content.application/json.schema.properties.billing_events.items.properties.tags
      before: 'null'
      after: '{"type":"object","additionalProperties":{"type":"string"},"description":"Activated X-Fal-Tags key/values set on this request. Only populated when source=tagged-billed; a key the request did not set is omitted, so a request that set none reports {}."}'
  date: '2026-09-23'
- change: changed
  target: route
  breaking: false
  deprecated: false
  doc_only: true
  note: Route description expanded to document tagged billing events, the 'source=tagged-billed' mode, and 'tag' filtering semantics.
  paths:
    - path: description
      before: '\nReturns paginated individual billing event records with filters\nfor endpoint and date range. Each record includes the request ID, timestamp,\nendpoint, output units billed, and a cost breakdown in USD (cost_subtotal,\ncost_discount, cost_total; cost_estimate_nano_usd carries cost_total in nano USD).\n\n**Key Features:**\n- Individual billing event records for each API request\n- Per-request cost breakdown before and after discounts\n- Flexible date range filtering\n- Optional endpoint filtering\n- Cursor-based pagination for efficient large dataset queries\n- Limited to 10000 records per page for performance\n- Date range capped at 90 days per request\n\n**Common Use Cases:**\n- Audit individual billing events\n- Track request patterns and volumes\n- Debug specific requests by ID\n- Monitor billing unit consumption per request\n\nSee [fal.ai docs](https://fal.ai/docs/documentation/model-apis/faq) for more details.\n    '
      after: '\nReturns paginated individual billing event records with filters\nfor endpoint and date range. Each record includes the request ID, timestamp,\nendpoint, output units billed, and a cost breakdown in USD (cost_subtotal,\ncost_discount, cost_total; cost_estimate_nano_usd carries cost_total in nano USD).\n\n**Key Features:**\n- Individual billing event records for each API request\n- Per-request cost breakdown before and after discounts\n- Flexible date range filtering\n- Optional endpoint filtering\n- Cursor-based pagination for efficient large dataset queries\n- Limited to 10000 records per page for performance\n- Date range capped at 90 days per request\n\n**Tagged billing events:** pass `source=tagged-billed` to read the tagged\nbilling-event pivot instead of the default feed. Each row then carries a `tags`\nobject built from the `X-Fal-Tags` tags set on that request, and\n`tag=key=value` filters narrow the results — repeating the parameter with\ndifferent keys ANDs them, repeating one key matches any of its values. Requires\ntagged reporting to be enabled for the account; recent events are delayed\nrelative to the default source.\n\n**Common Use Cases:**\n- Audit individual billing events\n- Track request patterns and volumes\n- Debug specific requests by ID\n- Monitor billing unit consumption per request\n\nSee [fal.ai docs](https://fal.ai/docs/documentation/model-apis/faq) for more details.\n    '
  date: '2026-09-23'
