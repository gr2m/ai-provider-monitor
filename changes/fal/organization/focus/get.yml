- change: added
  target: route
  breaking: false
  deprecated: false
  doc_only: false
  note: Adds GET /organization/focus to download a FOCUS-compliant billing CSV across the org (or a single team). Supports source=invoice (by billing_month or charge_month) and source=estimate (date-range). Requires admin API key on the org’s root team.
  paths: []
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/925
- change: added
  target: request
  breaking: false
  deprecated: false
  doc_only: false
  note: Added new 'tagged-estimate' option to the 'source' query parameter enum.
  paths:
    - path: parameters[0].schema.enum[2]
      before: 'null'
      after: tagged-estimate
  date: '2026-09-02'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/987
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: true
  note: Expanded 'source' parameter description to document 'tagged-estimate' behavior and requirements.
  paths:
    - path: parameters[0].description
      before: Report source. 'invoice' returns finalized invoice data for a billing month. 'estimate' returns real-time usage estimates for a date range.
      after: Report source. 'invoice' returns finalized invoice data for a billing month. 'estimate' returns real-time usage estimates for a date range. 'tagged-estimate' returns the same date range from the tagged usage aggregate, populating the Tags column; it requires tagged reporting to be enabled for the organization and at least one activated tag key, and its figures can differ from 'estimate' — the aggregate deduplicates by request, covers endpoint requests only, and refreshes on its own schedule rather than continuously.
    - path: parameters[0].schema.description
      before: Report source. 'invoice' returns finalized invoice data for a billing month. 'estimate' returns real-time usage estimates for a date range.
      after: Report source. 'invoice' returns finalized invoice data for a billing month. 'estimate' returns real-time usage estimates for a date range. 'tagged-estimate' returns the same date range from the tagged usage aggregate, populating the Tags column; it requires tagged reporting to be enabled for the organization and at least one activated tag key, and its figures can differ from 'estimate' — the aggregate deduplicates by request, covers endpoint requests only, and refreshes on its own schedule rather than continuously.
  date: '2026-09-02'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/987
- change: changed
  target: route
  breaking: false
  deprecated: false
  doc_only: true
  note: Route description updated to list three data sources and revised 'tagged-estimate' behavior/requirements.
  paths:
    - path: description
      before: |-

        Returns a FOCUS compliant billing report as a CSV download, spanning every team
        in your organization. Each invoiced billing customer is reported as a
        `BillingAccount`. Under shared (pooled) billing — one billing customer covering
        multiple teams — each row is additionally attributed to the calling team via the
        `SubAccountId` / `SubAccountName` columns. `SubAccountId` is that team's own
        billing customer ID, so both account columns share one identifier namespace.

        > **Availability:** This endpoint is available to enterprise customers with FOCUS reports and organizations enabled. Contact your account team or support@fal.ai to request access.

        Must be called with an admin API key on the organization's root team.

        Supports two data sources:
        - **estimate**: Real-time usage estimates for a date range. Under pooled billing every row is attributed to the calling team.
        - **invoice**: Finalized invoice data for a billing month. Under pooled billing, per-team `SubAccount` attribution is available on endpoint (Model API) lines that carry a caller; app and compute lines carry no caller and have no SubAccount.

        Use `team_username` to restrict the report to a single team. Under shared
        (pooled) billing this is rejected for `source=invoice` — the invoice is issued
        to one billing customer shared across teams and cannot be split per team; use
        `source=estimate` for per-team figures.

        **Invoice reports** default to the most recently available billing month.
        **Usage estimates** default to the last 24 hours, with a maximum 90-day date range.
            
      after: |-

        Returns a FOCUS compliant billing report as a CSV download, spanning every team
        in your organization. Each invoiced billing customer is reported as a
        `BillingAccount`. Under shared (pooled) billing — one billing customer covering
        multiple teams — each row is additionally attributed to the calling team via the
        `SubAccountId` / `SubAccountName` columns. `SubAccountId` is that team's own
        billing customer ID, so both account columns share one identifier namespace.

        > **Availability:** This endpoint is available to enterprise customers with FOCUS reports and organizations enabled. Contact your account team or support@fal.ai to request access.

        Must be called with an admin API key on the organization's root team.

        Supports three data sources:
        - **estimate**: Real-time usage estimates for a date range. Under pooled billing every row is attributed to the calling team.
        - **tagged-estimate**: Those same estimates with the `Tags` column populated from the `X-Fal-Tags` tags set on your requests. Requires tagged reporting to be enabled for the organization; recent usage is delayed relative to `estimate`.
        - **invoice**: Finalized invoice data for a billing month. Under pooled billing, per-team `SubAccount` attribution is available on endpoint (Model API) lines that carry a caller; app and compute lines carry no caller and have no SubAccount.

        Use `team_username` to restrict the report to a single team. Under shared
        (pooled) billing this is rejected for `source=invoice` — the invoice is issued
        to one billing customer shared across teams and cannot be split per team; use
        `source=estimate` for per-team figures.

        **Invoice reports** default to the most recently available billing month.
        **Usage estimates** default to the last 24 hours, with a maximum 90-day date range.
            
  date: '2026-09-03'
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: true
  note: Updated 'source' parameter description to clarify 'tagged-estimate' behavior and requirements (uses X-Fal-Tags; delayed relative to estimate).
  paths:
    - path: parameters[0].description
      before: Report source. 'invoice' returns finalized invoice data for a billing month. 'estimate' returns real-time usage estimates for a date range. 'tagged-estimate' returns the same date range from the tagged usage aggregate, populating the Tags column; it requires tagged reporting to be enabled for the organization and at least one activated tag key, and its figures can differ from 'estimate' — the aggregate deduplicates by request, covers endpoint requests only, and refreshes on its own schedule rather than continuously.
      after: Report source. 'invoice' returns finalized invoice data for a billing month. 'estimate' returns real-time usage estimates for a date range. 'tagged-estimate' returns those same estimates with the Tags column populated from the X-Fal-Tags tags set on your requests; it requires tagged reporting to be enabled for the organization, and recent usage is delayed relative to 'estimate'.
    - path: parameters[0].schema.description
      before: Report source. 'invoice' returns finalized invoice data for a billing month. 'estimate' returns real-time usage estimates for a date range. 'tagged-estimate' returns the same date range from the tagged usage aggregate, populating the Tags column; it requires tagged reporting to be enabled for the organization and at least one activated tag key, and its figures can differ from 'estimate' — the aggregate deduplicates by request, covers endpoint requests only, and refreshes on its own schedule rather than continuously.
      after: Report source. 'invoice' returns finalized invoice data for a billing month. 'estimate' returns real-time usage estimates for a date range. 'tagged-estimate' returns those same estimates with the Tags column populated from the X-Fal-Tags tags set on your requests; it requires tagged reporting to be enabled for the organization, and recent usage is delayed relative to 'estimate'.
  date: '2026-09-03'
