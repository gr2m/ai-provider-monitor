- change: added
  target: route
  breaking: false
  deprecated: false
  doc_only: false
  note: Adds GET /account/focus to download a FOCUS-compliant billing report (invoice or estimate) as CSV for enterprise customers.
  paths: []
  date: '2026-03-17'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/791
- change: added
  target: request
  breaking: false
  deprecated: false
  doc_only: false
  note: Added optional 'timezone' query parameter to control date bucketing timezone (default UTC).
  paths:
    - path: parameters[5]
      before: 'null'
      after: '{"description":"Timezone for date aggregation and boundaries. All timestamps in responses are in UTC, but this controls how dates are bucketed.","in":"query","name":"timezone","required":false,"schema":{"default":"UTC","description":"Timezone for date aggregation and boundaries. All timestamps in responses are in UTC, but this controls how dates are bucketed.","example":"UTC","type":"string"}}'
  date: '2026-03-17'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/798
- change: added
  target: request
  breaking: false
  deprecated: false
  doc_only: false
  note: Added optional 'timeframe' query parameter to select aggregation granularity.
  paths:
    - path: parameters[6]
      before: 'null'
      after: '{"description":"Aggregation timeframe for timeseries data (auto-detected from date range if not specified). Auto-detection uses: minute (<2h), hour (<2d), day (<64d), week (<183d), month (>=183d).","in":"query","name":"timeframe","required":false,"schema":{"description":"Aggregation timeframe for timeseries data (auto-detected from date range if not specified). Auto-detection uses: minute (<2h), hour (<2d), day (<64d), week (<183d), month (>=183d).","enum":["minute","hour","day","week","month"],"example":"day","type":"string"}}'
  date: '2026-03-17'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/798
- change: added
  target: request
  breaking: false
  deprecated: false
  doc_only: false
  note: Added optional 'bound_to_timeframe' query parameter to align dates to timeframe boundaries (default true).
  paths:
    - path: parameters[7]
      before: 'null'
      after: '{"description":"Whether to adjust start/end dates to align with timeframe boundaries and use exclusive end. Defaults to true. When true, dates are aligned to the start of the timeframe period (e.g., start of day) and end is made exclusive (e.g., start of next day). When false, uses exact dates provided.","in":"query","name":"bound_to_timeframe","required":false,"schema":{"default":"true","description":"Whether to adjust start/end dates to align with timeframe boundaries and use exclusive end. Defaults to true. When true, dates are aligned to the start of the timeframe period (e.g., start of day) and end is made exclusive (e.g., start of next day). When false, uses exact dates provided.","enum":["true","false"],"example":"true","type":"string"}}'
  date: '2026-03-17'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/798
- change: added
  target: request
  breaking: false
  deprecated: false
  doc_only: false
  note: Added optional query parameter expand=organization for org-wide reports with per-team attribution (org roots only).
  paths:
    - path: $.parameters[name=expand]
      before: 'null'
      after: 'Query param ''expand'' (enum: [''organization'']) added'
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/925
- change: changed
  target: route
  breaking: false
  deprecated: false
  doc_only: true
  note: 'Route description updated: clarified 90-day date range for estimates and documented org-wide expansion behavior under pooled billing.'
  paths:
    - path: $.description
      before: Usage estimates default to the last 24 hours, with a maximum 90-day lookback.
      after: Usage estimates default to the last 24 hours, with a maximum 90-day date range. Organization roots may pass expand=organization to report the whole organization (each team as a BillingAccount) with per-team SubAccount attribution under pooled billing; invoice source already spans pooled teams, expand adds per-team breakdown.
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/925
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: true
  note: Clarified billing_month semantics and default in description.
  paths:
    - path: $.parameters[name=billing_month].description
      before: Invoice billing month (YYYY-MM). The month the invoice was issued (e.g. '2025-02' for January charges). Used with source=invoice. Defaults to most recent available billing month.
      after: Billing month (YYYY-MM) — selects invoices by their issue date (when the invoice was issued). Used with source=invoice. Defaults to the current billing month.
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/925
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: true
  note: Clarified charge_month behavior to select by incurred period, including items split across invoices.
  paths:
    - path: $.parameters[name=charge_month].description
      before: Charge month (YYYY-MM). The month charges were incurred. Converted to billing_month by adding 1 month (billing in arrears). Alternative to billing_month. Used with source=invoice.
      after: Charge month (YYYY-MM) — selects line items by the period charges were incurred, capturing them even when split across invoices issued in different months. Alternative to billing_month. Used with source=invoice.
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/925
