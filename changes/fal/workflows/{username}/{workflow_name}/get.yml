- change: added
  target: request
  breaking: true
  deprecated: false
  doc_only: false
  note: Added maxLength (128) and allowed-character pattern for username path parameter
  paths:
    - path: parameters[0].schema.maxLength
      before: 'null'
      after: '128'
    - path: parameters[0].schema.pattern
      before: 'null'
      after: ^[a-zA-Z0-9_-]+$
  date: '2026-04-13'
- change: added
  target: request
  breaking: true
  deprecated: false
  doc_only: false
  note: Added maxLength (128) and allowed-character pattern for workflow_name path parameter
  paths:
    - path: parameters[1].schema.maxLength
      before: 'null'
      after: '128'
    - path: parameters[1].schema.pattern
      before: 'null'
      after: ^[a-zA-Z0-9_-]+$
  date: '2026-04-13'
