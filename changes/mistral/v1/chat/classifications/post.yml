- change: added
  target: request
  breaking: false
  deprecated: false
  doc_only: false
  note: SystemMessage content array now supports 'thinking' (ThinkChunk) in single-input requests.
  paths:
    - path: requestBody.content.application/json.schema.properties.input.anyOf[0].properties.messages.items.oneOf[0].properties.content.anyOf[1].items
      before: items was a single TextChunk object (no discriminator)
      after: 'items now has a discriminator (type) with oneOf: [TextChunk, ThinkChunk]'
  date: '2026-02-20'
- change: added
  target: request
  breaking: false
  deprecated: false
  doc_only: false
  note: SystemMessage content array now supports 'thinking' (ThinkChunk) in batched-input requests.
  paths:
    - path: requestBody.content.application/json.schema.properties.input.anyOf[1].items.properties.messages.items.oneOf[0].properties.content.anyOf[1].items
      before: items was a single TextChunk object (no discriminator)
      after: 'items now has a discriminator (type) with oneOf: [TextChunk, ThinkChunk]'
  date: '2026-02-20'
