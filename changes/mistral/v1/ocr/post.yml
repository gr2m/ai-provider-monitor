- change: added
  target: request
  breaking: false
  deprecated: false
  doc_only: false
  note: Added document_annotation_prompt to guide structured extraction for the whole document
  paths:
    - path: requestBody.content.application/json.schema.properties.document_annotation_prompt
      before: 'null'
      after: '{"anyOf":[{"type":"string"},{"type":"null"}],"title":"Document Annotation Prompt"}'
  date: '2026-02-20'
- change: added
  target: request
  breaking: false
  deprecated: false
  doc_only: false
  note: Added extract_header and extract_footer flags to optionally extract page headers and footers
  paths:
    - path: requestBody.content.application/json.schema.properties.extract_header
      before: 'null'
      after: '{"type":"boolean","default":false}'
    - path: requestBody.content.application/json.schema.properties.extract_footer
      before: 'null'
      after: '{"type":"boolean","default":false}'
  date: '2026-02-20'
- change: added
  target: request
  breaking: false
  deprecated: false
  doc_only: false
  note: Added table_format option to control table output format (markdown or html)
  paths:
    - path: requestBody.content.application/json.schema.properties.table_format
      before: 'null'
      after: '{"anyOf":[{"type":"string","enum":["markdown","html"]},{"type":"null"}],"title":"Table Format"}'
  date: '2026-02-20'
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: true
  note: 'Updated docs for bbox_annotation_format ResponseFormat: moved description to object, added examples, and removed description from type property'
  paths:
    - path: requestBody.content.application/json.schema.properties.bbox_annotation_format.anyOf[0].description
      before: 'null'
      after: 'Specify the format that the model must output. By default it will use `{ "type": "text" }`. Setting to `{ "type": "json_object" }` enables JSON mode, which guarantees the message the model generates is in JSON. When using JSON mode you MUST also instruct the model to produce JSON yourself with a system or a user message. Setting to `{ "type": "json_schema" }` enables JSON schema mode, which guarantees the message the model generates is in JSON and follows the schema you provide.'
    - path: requestBody.content.application/json.schema.properties.bbox_annotation_format.anyOf[0].examples
      before: 'null'
      after: '[{"type":"text"},{"type":"json_object"},{"type":"json_schema","json_schema":{"name":"book","schema":{"type":"object"},"strict":true}}]'
    - path: requestBody.content.application/json.schema.properties.bbox_annotation_format.anyOf[0].properties.type.description
      before: 'An object specifying the format that the model must output. Setting to `{ "type": "json_object" }` enables JSON mode, which guarantees the message the model generates is in JSON. When using JSON mode you MUST also instruct the model to produce JSON yourself with a system or a user message.'
      after: 'null'
  date: '2026-02-20'
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: true
  note: 'Updated docs for document_annotation_format ResponseFormat: moved description to object, added examples, and removed description from type property'
  paths:
    - path: requestBody.content.application/json.schema.properties.document_annotation_format.anyOf[0].description
      before: 'null'
      after: 'Specify the format that the model must output. By default it will use `{ "type": "text" }`. Setting to `{ "type": "json_object" }` enables JSON mode, which guarantees the message the model generates is in JSON. When using JSON mode you MUST also instruct the model to produce JSON yourself with a system or a user message. Setting to `{ "type": "json_schema" }` enables JSON schema mode, which guarantees the message the model generates is in JSON and follows the schema you provide.'
    - path: requestBody.content.application/json.schema.properties.document_annotation_format.anyOf[0].examples
      before: 'null'
      after: '[{"type":"text"},{"type":"json_object"},{"type":"json_schema","json_schema":{"name":"book","schema":{"type":"object"},"strict":true}}]'
    - path: requestBody.content.application/json.schema.properties.document_annotation_format.anyOf[0].properties.type.description
      before: 'An object specifying the format that the model must output. Setting to `{ "type": "json_object" }` enables JSON mode, which guarantees the message the model generates is in JSON. When using JSON mode you MUST also instruct the model to produce JSON yourself with a system or a user message.'
      after: 'null'
  date: '2026-02-20'
- change: added
  target: response
  breaking: false
  deprecated: false
  doc_only: false
  note: Added header, footer, hyperlinks, and tables to each page in the OCR response
  paths:
    - path: responses.200.content.application/json.schema.properties.pages.items.properties.header
      before: 'null'
      after: '{"anyOf":[{"type":"string"},{"type":"null"}],"title":"Header"}'
    - path: responses.200.content.application/json.schema.properties.pages.items.properties.footer
      before: 'null'
      after: '{"anyOf":[{"type":"string"},{"type":"null"}],"title":"Footer"}'
    - path: responses.200.content.application/json.schema.properties.pages.items.properties.hyperlinks
      before: 'null'
      after: '{"type":"array","items":{"type":"string"},"title":"Hyperlinks"}'
    - path: responses.200.content.application/json.schema.properties.pages.items.properties.tables
      before: 'null'
      after: '{"type":"array","items":{"type":"object","title":"OCRTableObject","properties":{"id":{"type":"string"},"content":{"type":"string"},"format":{"type":"string","enum":["markdown","html"]}},"required":["id","content","format"]},"title":"Tables"}'
  date: '2026-02-20'
- change: added
  target: response
  breaking: false
  deprecated: false
  doc_only: true
  note: Added example payload for 200 response
  paths:
    - path: responses.200.content.application/json.examples.userExample
      before: 'null'
      after: <example object added>
  date: '2026-02-20'
