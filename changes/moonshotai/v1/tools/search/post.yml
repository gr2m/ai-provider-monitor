- change: added
  target: route
  breaking: false
  deprecated: false
  doc_only: false
  note: Added POST /v1/tools/search to run web searches and return structured results (title, snippet, site, URL). Supports include_content to return full page text, limit (1–20, default 5), and optional timeout. Billed only when at least one result is returned. Returns X-Msh-Track-Id and X-Msh-Chat-Id headers.
  paths: []
  date: '2026-09-15'
