- change: added
  target: route
  breaking: false
  deprecated: false
  doc_only: false
  note: Adds POST /vector_stores/{vector_store_id}/file_batches to create a batch of files for a vector store from file_ids or per-file objects with optional attributes and chunking strategies; returns batch metadata, status, and file counts.
  paths: []
  date: '2026-02-20'
  diff_url: https://github.com/gr2m/ai-provider-api-changes/pull/578
- change: added
  target: route
  breaking: false
  deprecated: false
  doc_only: true
  note: Added route-level description noting the 2000-file maximum per batch.
  paths:
    - path: $.description
      before: 'null'
      after: The maximum number of files in a single batch request is 2000.
  date: '2026-02-20'
  diff_url: https://github.com/gr2m/ai-provider-api-changes/pull/622
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: false
  note: Increased maximum items for file_ids from 500 to 2000; description updated to mention the new limit.
  paths:
    - path: $.requestBody.content.application/json.schema.properties.file_ids.maxItems
      before: '500'
      after: '2000'
    - path: $.requestBody.content.application/json.schema.properties.file_ids.description
      before: A list of [File](/docs/api-reference/files) IDs that the vector store should use. Useful for tools like `file_search` that can access files.  If `attributes` or `chunking_strategy` are provided, they will be  applied to all files in the batch. Mutually exclusive with `files`.
      after: A list of [File](/docs/api-reference/files) IDs that the vector store should use. Useful for tools like `file_search` that can access files.  If `attributes` or `chunking_strategy` are provided, they will be  applied to all files in the batch. The maximum batch size is 2000 files. Mutually exclusive with `files`.
  date: '2026-02-20'
  diff_url: https://github.com/gr2m/ai-provider-api-changes/pull/622
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: false
  note: Increased maximum items for files from 500 to 2000; description updated to mention the new limit.
  paths:
    - path: $.requestBody.content.application/json.schema.properties.files.maxItems
      before: '500'
      after: '2000'
    - path: $.requestBody.content.application/json.schema.properties.files.description
      before: A list of objects that each include a `file_id` plus optional `attributes` or `chunking_strategy`. Use this when you need to override metadata for specific files. The global `attributes` or `chunking_strategy` will be ignored and must be specified for each file. Mutually exclusive with `file_ids`.
      after: A list of objects that each include a `file_id` plus optional `attributes` or `chunking_strategy`. Use this when you need to override metadata for specific files. The global `attributes` or `chunking_strategy` will be ignored and must be specified for each file. The maximum batch size is 2000 files. Mutually exclusive with `file_ids`.
  date: '2026-02-20'
  diff_url: https://github.com/gr2m/ai-provider-api-changes/pull/622
- change: changed
  target: route
  breaking: false
  deprecated: false
  doc_only: true
  note: Route description updated to include per-vector-store rate limits and recommendation to use batch ingestion.
  paths:
    - path: description
      before: The maximum number of files in a single batch request is 2000.
      after: |
        The maximum number of files in a single batch request is 2000.
        Vector store file attach requests are rate limited per vector store (300 requests per minute across both this endpoint and `/vector_stores/{vector_store_id}/files`).
        For ingesting multiple files into the same vector store, this batch endpoint is recommended.
  date: '2026-03-26'
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: true
  note: Updated description for file_ids to recommend batch endpoint for multi-file ingestion and mention reduced per-vector-store write pressure.
  paths:
    - path: components.schemas.CreateVectorStoreFileBatchRequest.properties.file_ids.description
      before: A list of [File](/docs/api-reference/files) IDs that the vector store should use. Useful for tools like `file_search` that can access files.  If `attributes` or `chunking_strategy` are provided, they will be  applied to all files in the batch. The maximum batch size is 2000 files. Mutually exclusive with `files`.
      after: A list of [File](/docs/api-reference/files) IDs that the vector store should use. Useful for tools like `file_search` that can access files.  If `attributes` or `chunking_strategy` are provided, they will be  applied to all files in the batch. The maximum batch size is 2000 files. This endpoint is recommended for multi-file ingestion and helps reduce per-vector-store write request pressure. Mutually exclusive with `files`.
  date: '2026-03-26'
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: true
  note: Updated description for files to recommend batch endpoint for multi-file ingestion and mention reduced per-vector-store write pressure.
  paths:
    - path: components.schemas.CreateVectorStoreFileBatchRequest.properties.files.description
      before: A list of objects that each include a `file_id` plus optional `attributes` or `chunking_strategy`. Use this when you need to override metadata for specific files. The global `attributes` or `chunking_strategy` will be ignored and must be specified for each file. The maximum batch size is 2000 files. Mutually exclusive with `file_ids`.
      after: A list of objects that each include a `file_id` plus optional `attributes` or `chunking_strategy`. Use this when you need to override metadata for specific files. The global `attributes` or `chunking_strategy` will be ignored and must be specified for each file. The maximum batch size is 2000 files. This endpoint is recommended for multi-file ingestion and helps reduce per-vector-store write request pressure. Mutually exclusive with `file_ids`.
  date: '2026-03-26'
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: true
  note: Updated description for nested CreateVectorStoreFileRequest.file_id to recommend using file_batches for multi-file ingestion to minimize per-vector-store write requests.
  paths:
    - path: components.schemas.CreateVectorStoreFileRequest.properties.file_id.description
      before: A [File](/docs/api-reference/files) ID that the vector store should use. Useful for tools like `file_search` that can access files.
      after: A [File](/docs/api-reference/files) ID that the vector store should use. Useful for tools like `file_search` that can access files. For multi-file ingestion, we recommend [`file_batches`](/docs/api-reference/vector-stores-file-batches/createBatch) to minimize per-vector-store write requests.
  date: '2026-03-26'
