- change: added
  target: route
  breaking: false
  deprecated: false
  doc_only: false
  note: New POST /vector_stores endpoint to create a vector store, with optional files, metadata, name/description, chunking strategy, and expiration; returns the created vector store with status and file counts.
  paths: []
  date: '2026-02-20'
  diff_url: https://github.com/gr2m/ai-provider-api-changes/pull/578
- change: changed
  target: response
  breaking: false
  deprecated: false
  doc_only: false
  note: Added 'unixtime' format to integer timestamp fields in VectorStoreObject (created_at, expires_at, last_active_at).
  paths:
    - path: components.schemas.VectorStoreObject.properties.created_at.format
      before: 'null'
      after: unixtime
    - path: components.schemas.VectorStoreObject.properties.expires_at.anyOf[0].format
      before: 'null'
      after: unixtime
    - path: components.schemas.VectorStoreObject.properties.last_active_at.anyOf[0].format
      before: 'null'
      after: unixtime
  date: '2026-08-24'
