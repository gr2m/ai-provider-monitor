- change: added
  target: route
  breaking: false
  deprecated: false
  doc_only: false
  note: Introduces POST /chat/completions/{completion_id} to modify a stored chat completion by ID. Only completions created with store=true can be edited, and the only supported update is to the metadata field.
  paths: []
  date: '2026-02-20'
  diff_url: https://github.com/gr2m/ai-provider-api-changes/pull/578
- change: added
  target: response
  breaking: false
  deprecated: false
  doc_only: false
  note: Added optional moderation field to chat completion response.
  paths:
    - path: components/schemas/CreateChatCompletionResponse/properties/moderation
      before: 'null'
      after: anyOf[#/components/schemas/ChatCompletionModeration, null]
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/942
- change: added
  target: response
  breaking: false
  deprecated: false
  doc_only: false
  note: Added unixtime format to created timestamp in response.
  paths:
    - path: components/schemas/CreateChatCompletionResponse/properties/created/format
      before: 'null'
      after: unixtime
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/942
- change: added
  target: response
  breaking: false
  deprecated: false
  doc_only: false
  note: Added uri format to annotations.url_citation.url.
  paths:
    - path: components/schemas/ChatCompletionResponseMessage/properties/annotations/items/properties/url_citation/properties/url/format
      before: 'null'
      after: uri
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/942
- change: added
  target: response
  breaking: false
  deprecated: false
  doc_only: false
  note: Added unixtime format to audio.expires_at.
  paths:
    - path: components/schemas/ChatCompletionResponseMessage/properties/audio/anyOf/0/properties/expires_at/format
      before: 'null'
      after: unixtime
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/942
- change: changed
  target: response
  breaking: true
  deprecated: false
  doc_only: false
  note: ServiceTier enum now includes 'fast'.
  paths:
    - path: components/schemas/ServiceTier/anyOf/0/enum
      before: '["auto","default","flex","scale","priority"]'
      after: '["auto","default","flex","scale","priority","fast"]'
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/942
- change: added
  target: response
  breaking: false
  deprecated: false
  doc_only: false
  note: Added cache_write_tokens to prompt_tokens_details in usage.
  paths:
    - path: components/schemas/CompletionUsage/properties/prompt_tokens_details/properties/cache_write_tokens
      before: 'null'
      after: '{"type":"integer","default":0,"description":"The unadjusted number of prompt tokens written to cache."}'
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/942
- change: changed
  target: response
  breaking: false
  deprecated: false
  doc_only: true
  note: Updated finish_reason description with link to Model Spec.
  paths:
    - path: components/schemas/CreateChatCompletionResponse/properties/choices/items/properties/finish_reason/description
      before: |
        The reason the model stopped generating tokens. This will be `stop` if the model hit a natural stop point or a provided stop sequence,
        `length` if the maximum number of tokens specified in the request was reached,
        `content_filter` if content was omitted due to a flag from our content filters,
        `tool_calls` if the model called a tool, or `function_call` (deprecated) if the model called a function.
      after: |
        The reason the model stopped generating tokens. This will be `stop` if the model hit a natural stop point or a provided stop sequence,
        `length` if the maximum number of tokens specified in the request was reached,
        `content_filter` if content was omitted due to a flag from our content filters,
        `tool_calls` if the model called a tool, or `function_call` (deprecated) if the model called a function.
        Read the [Model Spec](https://model-spec.openai.com/2025-12-18.html) for more.
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/942
- change: changed
  target: response
  breaking: false
  deprecated: false
  doc_only: true
  note: Reworded description for top_logprobs in token logprobs.
  paths:
    - path: components/schemas/ChatCompletionTokenLogprob/properties/top_logprobs/description
      before: List of the most likely tokens and their log probability, at this token position. In rare cases, there may be fewer than the number of requested `top_logprobs` returned.
      after: List of the most likely tokens and their log probability, at this token position. The number of entries may be fewer than the requested `top_logprobs`.
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/942
- change: changed
  target: response
  breaking: false
  deprecated: false
  doc_only: true
  note: Updated ServiceTier description to document Fast mode behavior and Flex wording.
  paths:
    - path: components/schemas/ServiceTier/anyOf/0/description
      before: |
        Specifies the processing type used for serving the request.
          - If set to 'auto', then the request will be processed with the service tier configured in the Project settings. Unless otherwise configured, the Project will use 'default'.
          - If set to 'default', then the request will be processed with the standard pricing and performance for the selected model.
          - If set to '[flex](/docs/guides/flex-processing)' or '[priority](https://openai.com/api-priority-processing/)', then the request will be processed with the corresponding service tier.
          - When not set, the default behavior is 'auto'.

          When the `service_tier` parameter is set, the response body will include the `service_tier` value based on the processing mode actually used to serve the request. This response value may be different from the value set in the parameter.
      after: |
        Specifies the processing type used for serving the request.
          - If set to 'auto', then the request will be processed with the service tier configured in the Project settings. Unless otherwise configured, the Project will use 'default'.
          - If set to 'default', then the request will be processed with the standard pricing and performance for the selected model.
          - If set to '[flex](/docs/guides/flex-processing)', then the request will be processed with the Flex Processing service tier.
          - To opt-in to [Fast mode](/api/docs/guides/fast-mode) at the request level, include the `service_tier=fast` or `service_tier=priority` parameter for Responses or Chat Completions. The response will show `service_tier=priority` regardless of if you specify `service_tier=fast` or `priority` in your request.
          - When not set, the default behavior is 'auto'.

          When the `service_tier` parameter is set, the response body will include the `service_tier` value based on the processing mode actually used to serve the request. This response value may be different from the value set in the parameter.
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/942
