- change: added
  target: route
  breaking: false
  deprecated: false
  doc_only: false
  note: Adds POST /realtime/calls to initiate a WebRTC Realtime call by submitting an SDP offer and receiving a 201 SDP answer; supports raw application/sdp or multipart form with optional session configuration, and returns a Location header with the call ID for follow-up control.
  paths: []
  date: '2026-02-20'
  diff_url: https://github.com/gr2m/ai-provider-api-changes/pull/578
- change: added
  target: request
  breaking: false
  deprecated: false
  doc_only: false
  note: Added new allowed model enum value 'gpt-realtime-1.5' in session.model.
  paths:
    - path: requestBody.content["multipart/form-data"].schema.properties.session.allOf[0].properties.model.anyOf[1].enum[1]
      before: 'null'
      after: gpt-realtime-1.5
  date: '2026-02-23'
  diff_url: https://github.com/gr2m/ai-provider-api-changes/pull/694
- change: added
  target: request
  breaking: false
  deprecated: false
  doc_only: false
  note: Added new allowed model enum value 'gpt-audio-1.5' in session.model.
  paths:
    - path: requestBody.content["multipart/form-data"].schema.properties.session.allOf[0].properties.model.anyOf[1].enum[12]
      before: 'null'
      after: gpt-audio-1.5
  date: '2026-02-23'
  diff_url: https://github.com/gr2m/ai-provider-api-changes/pull/694
- change: added
  target: request
  breaking: false
  deprecated: false
  doc_only: false
  note: Added optional boolean property `defer_loading` to MCP tool configuration to allow deferred tool discovery via search.
  paths:
    - path: components/schemas/MCPTool/properties/defer_loading
      before: 'null'
      after: '{"description":"Whether this MCP tool is deferred and discovered via tool search.","type":"boolean"}'
  date: '2026-03-05'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/749
- change: added
  target: request
  breaking: false
  deprecated: false
  doc_only: false
  note: Introduced new schema `FileInputDetail` with allowed values `low` and `high`.
  paths:
    - path: components/schemas/FileInputDetail
      before: 'null'
      after: '{"enum":["low","high"],"type":"string"}'
  date: '2026-03-05'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/749
- change: added
  target: request
  breaking: false
  deprecated: false
  doc_only: false
  note: Added optional `detail` property to `InputFileContent` referencing `FileInputDetail` (defaults to high).
  paths:
    - path: components/schemas/InputFileContent/properties/detail
      before: 'null'
      after: '{"$ref":"#/components/schemas/FileInputDetail","description":"The detail level of the file to be sent to the model. One of `high` or `low`. Defaults to `high`."}'
  date: '2026-03-05'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/749
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: false
  note: Expanded `ImageDetail` enum to include new value `original`.
  paths:
    - path: components/schemas/ImageDetail/enum
      before: '["low","high","auto"]'
      after: '["low","high","auto","original"]'
  date: '2026-03-05'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/749
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: true
  note: Updated description of `InputImageContent.detail` to mention new `original` option.
  paths:
    - path: components/schemas/InputImageContent/properties/detail/description
      before: The detail level of the image to be sent to the model. One of `high`, `low`, or `auto`. Defaults to `auto`.
      after: The detail level of the image to be sent to the model. One of `high`, `low`, `auto`, or `original`. Defaults to `auto`.
  date: '2026-03-05'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/749
- change: removed
  target: request
  breaking: true
  deprecated: false
  doc_only: false
  note: Removed the 'detail' property from InputFileContent; file inputs no longer accept a detail level.
  paths:
    - path: components.schemas.InputFileContent.properties.detail
      before: '{"$ref":"#/components/schemas/FileInputDetail","description":"The detail level of the file to be sent to the model. One of `high` or `low`. Defaults to `high`."}'
      after: 'null'
  date: '2026-03-10'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/762
- change: removed
  target: request
  breaking: true
  deprecated: false
  doc_only: false
  note: Removed the FileInputDetail schema (previously used by InputFileContent.detail).
  paths:
    - path: components.schemas.FileInputDetail
      before: '{"enum":["low","high"],"type":"string"}'
      after: 'null'
  date: '2026-03-10'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/762
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: false
  note: VoiceIdsOrCustomVoice first anyOf entry simplified to directly reference VoiceIdsShared instead of an allOf with an extra type constraint; no functional impact.
  paths:
    - path: components.schemas.VoiceIdsOrCustomVoice.anyOf[0]
      before: '{"allOf":[{"$ref":"#/components/schemas/VoiceIdsShared"}],"type":"string"}'
      after: '{"$ref":"#/components/schemas/VoiceIdsShared"}'
  date: '2026-03-13'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/774
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: true
  note: Updated tracing field description to use an absolute Traces Dashboard URL.
  paths:
    - path: components/schemas/RealtimeSessionCreateRequestGA/properties/tracing/description
      before: |
        Realtime API can write session traces to the [Traces Dashboard](/logs?api=traces). Set to null to disable tracing. Once
        tracing is enabled for a session, the configuration cannot be modified.

        `auto` will create a trace for the session with default values for the
        workflow name, group id, and metadata.
      after: |
        Realtime API can write session traces to the [Traces Dashboard](https://platform.openai.com/logs?api=traces). Set to null to disable tracing. Once
        tracing is enabled for a session, the configuration cannot be modified.

        `auto` will create a trace for the session with default values for the
        workflow name, group id, and metadata.
  date: '2026-03-27'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/840
- change: added
  target: request
  breaking: false
  deprecated: false
  doc_only: false
  note: Added optional detail property to Input file content to control render quality (low/high).
  paths:
    - path: components.schemas.InputFileContent.properties.detail
      before: 'null'
      after: '{"$ref":"#/components/schemas/FileInputDetail","description":"The detail level of the file to be sent to the model. Use `low` for the default rendering behavior, or `high` to render the file at higher quality. Defaults to `low`."}'
  date: '2026-04-10'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/884
- change: added
  target: request
  breaking: false
  deprecated: false
  doc_only: false
  note: Introduced new FileInputDetail enum schema with values low and high.
  paths:
    - path: components.schemas.FileInputDetail
      before: 'null'
      after: '{"enum":["low","high"],"type":"string"}'
  date: '2026-04-10'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/884
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: false
  note: Expanded allowed models for session.model to include gpt-realtime-2, 2.1, and 2.1-mini.
  paths:
    - path: components.schemas.RealtimeSessionCreateRequestGA.properties.model.anyOf[1].enum
      before: '["gpt-realtime","gpt-realtime-1.5","gpt-realtime-2025-08-28","gpt-4o-realtime-preview","gpt-4o-realtime-preview-2024-10-01","gpt-4o-realtime-preview-2024-12-17","gpt-4o-realtime-preview-2025-06-03","gpt-4o-mini-realtime-preview","gpt-4o-mini-realtime-preview-2024-12-17","gpt-realtime-mini","gpt-realtime-mini-2025-10-06","gpt-realtime-mini-2025-12-15","gpt-audio-1.5","gpt-audio-mini","gpt-audio-mini-2025-10-06","gpt-audio-mini-2025-12-15"]'
      after: '["gpt-realtime","gpt-realtime-1.5","gpt-realtime-2","gpt-realtime-2.1","gpt-realtime-2.1-mini","gpt-realtime-2025-08-28","gpt-4o-realtime-preview","gpt-4o-realtime-preview-2024-10-01","gpt-4o-realtime-preview-2024-12-17","gpt-4o-realtime-preview-2025-06-03","gpt-4o-mini-realtime-preview","gpt-4o-mini-realtime-preview-2024-12-17","gpt-realtime-mini","gpt-realtime-mini-2025-10-06","gpt-realtime-mini-2025-12-15","gpt-audio-1.5","gpt-audio-mini","gpt-audio-mini-2025-10-06","gpt-audio-mini-2025-12-15"]'
  date: '2026-08-24'
- change: added
  target: request
  breaking: false
  deprecated: false
  doc_only: false
  note: Added parallel_tool_calls to control parallel tool invocation (reasoning models only).
  paths:
    - path: components.schemas.RealtimeSessionCreateRequestGA.properties.parallel_tool_calls
      before: 'null'
      after: '{"type":"boolean","description":"Whether the model may call multiple tools in parallel. Only supported by reasoning Realtime models such as `gpt-realtime-2`."}'
  date: '2026-08-24'
- change: added
  target: request
  breaking: false
  deprecated: false
  doc_only: false
  note: Added reasoning configuration for reasoning-capable models via session.reasoning.
  paths:
    - path: components.schemas.RealtimeSessionCreateRequestGA.properties.reasoning
      before: 'null'
      after: '{"$ref":"#/components/schemas/RealtimeReasoning"}'
    - path: components.schemas.RealtimeReasoning
      before: 'null'
      after: '{"type":"object","title":"Realtime reasoning configuration","description":"Configuration for reasoning-capable Realtime models such as `gpt-realtime-2`.","properties":{"effort":{"$ref":"#/components/schemas/RealtimeReasoningEffort"}}}'
    - path: components.schemas.RealtimeReasoningEffort.enum
      before: 'null'
      after: '["minimal","low","medium","high","xhigh"]'
  date: '2026-08-24'
- change: added
  target: request
  breaking: false
  deprecated: false
  doc_only: false
  note: 'Audio transcription config gained new tuning fields: delay, keywords, and languages.'
  paths:
    - path: components.schemas.AudioTranscription.properties.delay
      before: 'null'
      after: '{"type":"string","enum":["minimal","low","medium","high","xhigh"],"description":"Controls how long the model waits before emitting transcription text. Higher values can improve transcription accuracy at the cost of latency. Only supported with `gpt-realtime-whisper` in GA Realtime sessions."}'
    - path: components.schemas.AudioTranscription.properties.keywords
      before: 'null'
      after: '{"type":"array","items":{"type":"string"},"description":"Words or phrases to guide transcription of the input audio. Supported by `gpt-transcribe` and `gpt-live-transcribe`."}'
    - path: components.schemas.AudioTranscription.properties.languages
      before: 'null'
      after: '{"type":"array","minItems":1,"items":{"type":"string"},"description":"Possible languages of the input audio, in ISO-639-1 format. Supported by `gpt-transcribe` and `gpt-live-transcribe`."}'
  date: '2026-08-24'
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: false
  note: Expanded AudioTranscription.model enum to include gpt-transcribe, gpt-live-transcribe, and gpt-realtime-whisper.
  paths:
    - path: components.schemas.AudioTranscription.properties.model.anyOf[1].enum
      before: '["whisper-1","gpt-4o-mini-transcribe","gpt-4o-mini-transcribe-2025-12-15","gpt-4o-transcribe","gpt-4o-transcribe-diarize"]'
      after: '["whisper-1","gpt-transcribe","gpt-live-transcribe","gpt-4o-mini-transcribe","gpt-4o-mini-transcribe-2025-12-15","gpt-4o-transcribe","gpt-4o-transcribe-diarize","gpt-realtime-whisper"]'
  date: '2026-08-24'
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: true
  note: AudioTranscription.prompt description updated to note prompt is not supported with gpt-realtime-whisper in GA Realtime sessions.
  paths:
    - path: components.schemas.AudioTranscription.properties.prompt.description
      before: |
        An optional text to guide the model's style or continue a previous audio
        segment.
        For `whisper-1`, the [prompt is a list of keywords](/docs/guides/speech-to-text#prompting).
        For `gpt-4o-transcribe` models (excluding `gpt-4o-transcribe-diarize`), the prompt is a free text string, for example "expect words related to technology".
      after: |
        An optional text to guide the model's style or continue a previous audio
        segment.
        For `whisper-1`, the [prompt is a list of keywords](/docs/guides/speech-to-text#prompting).
        For `gpt-4o-transcribe` models (excluding `gpt-4o-transcribe-diarize`), the prompt is a free text string, for example "expect words related to technology".
        Prompt is not supported with `gpt-realtime-whisper` in GA Realtime sessions.
  date: '2026-08-24'
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: true
  note: 'Realtime turn detection docs updated: for gpt-realtime-whisper transcription sessions, turn_detection must be null (no VAD).'
  paths:
    - path: components.schemas.RealtimeTurnDetection.anyOf[0].description
      before: |
        Configuration for turn detection, ether Server VAD or Semantic VAD. This can be set to `null` to turn off, in which case the client must manually trigger model response.

        Server VAD means that the model will detect the start and end of speech based on audio volume and respond at the end of user speech.

        Semantic VAD is more advanced and uses a turn detection model (in conjunction with VAD) to semantically estimate whether the user has finished speaking, then dynamically sets a timeout based on this probability. For example, if user audio trails off with "uhhm", the model will score a low probability of turn end and wait longer for the user to continue speaking. This can be useful for more natural conversations, but may have a higher latency.
      after: |
        Configuration for turn detection, ether Server VAD or Semantic VAD. This can be set to `null` to turn off, in which case the client must manually trigger model response.

        Server VAD means that the model will detect the start and end of speech based on audio volume and respond at the end of user speech.

        Semantic VAD is more advanced and uses a turn detection model (in conjunction with VAD) to semantically estimate whether the user has finished speaking, then dynamically sets a timeout based on this probability. For example, if user audio trails off with "uhhm", the model will score a low probability of turn end and wait longer for the user to continue speaking. This can be useful for more natural conversations, but may have a higher latency.

        For `gpt-realtime-whisper` transcription sessions, turn detection must be
        set to `null`; VAD is not supported.
  date: '2026-08-24'
- change: added
  target: request
  breaking: false
  deprecated: false
  doc_only: false
  note: Added prompt_cache_breakpoint to input content types for precise prompt caching control.
  paths:
    - path: components.schemas.InputTextContent.properties.prompt_cache_breakpoint
      before: 'null'
      after: '{"$ref":"#/components/schemas/PromptCacheBreakpointConfig"}'
    - path: components.schemas.InputImageContent.properties.prompt_cache_breakpoint
      before: 'null'
      after: '{"$ref":"#/components/schemas/PromptCacheBreakpointConfig"}'
    - path: components.schemas.InputFileContent.properties.prompt_cache_breakpoint
      before: 'null'
      after: '{"$ref":"#/components/schemas/PromptCacheBreakpointConfig"}'
    - path: components.schemas.PromptCacheBreakpointConfig
      before: 'null'
      after: '{"type":"object","title":"Prompt cache breakpoint","description":"Marks the exact end of a reusable prompt prefix. The breakpoint inherits its TTL from the request''s `prompt_cache_options.ttl`; the boundary is not rounded to a token block.","properties":{"mode":{"type":"string","enum":["explicit"],"default":"explicit","description":"The breakpoint mode. Always `explicit`."}},"required":["mode"]}'
  date: '2026-08-24'
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: false
  note: image_url now annotated as a URI.
  paths:
    - path: components.schemas.InputImageContent.properties.image_url.anyOf[0].format
      before: 'null'
      after: '"uri"'
  date: '2026-08-24'
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: false
  note: file_url now annotated as a URI.
  paths:
    - path: components.schemas.InputFileContent.properties.file_url.format
      before: 'null'
      after: '"uri"'
  date: '2026-08-24'
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: false
  note: File input detail enum expanded to include 'auto'.
  paths:
    - path: components.schemas.FileInputDetail.enum
      before: '["low","high"]'
      after: '["auto","low","high"]'
  date: '2026-08-24'
- change: added
  target: request
  breaking: false
  deprecated: false
  doc_only: false
  note: MCP tool now supports restricting allowed_callers (direct/programmatic).
  paths:
    - path: components.schemas.MCPTool.properties.allowed_callers
      before: 'null'
      after: '{"anyOf":[{"type":"array","minItems":1,"items":{"$ref":"#/components/schemas/CallableToolAllowedCaller"}},{"type":"null"}] }'
  date: '2026-08-24'
- change: added
  target: request
  breaking: false
  deprecated: false
  doc_only: false
  note: MCP tool supports Secure MCP Tunnel via tunnel_id.
  paths:
    - path: components.schemas.MCPTool.properties.tunnel_id
      before: 'null'
      after: '{"type":"string","pattern":"^tunnel_[a-z0-9]{32}$","description":"The Secure MCP Tunnel ID to use instead of a direct server URL. One of `server_url`, `connector_id`, or `tunnel_id` must be provided."}'
  date: '2026-08-24'
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: false
  note: MCP server_url now annotated as a URI.
  paths:
    - path: components.schemas.MCPTool.properties.server_url.format
      before: 'null'
      after: '"uri"'
  date: '2026-08-24'
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: true
  note: MCP server location requirement docs updated to allow server_url, connector_id, or tunnel_id.
  paths:
    - path: components.schemas.MCPTool.properties.server_url.description
      before: |
        The URL for the MCP server. One of `server_url` or `connector_id` must be
        provided.
      after: |
        The URL for the MCP server. One of `server_url`, `connector_id`, or
        `tunnel_id` must be provided.
  date: '2026-08-24'
