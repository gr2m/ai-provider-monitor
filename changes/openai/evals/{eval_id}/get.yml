- change: added
  target: route
  breaking: false
  deprecated: false
  doc_only: false
  note: Introduces GET /evals/{eval_id} to retrieve an evaluation by ID, returning an Eval object (id, name, created_at, metadata) with data_source_config and testing_criteria.
  paths: []
  date: '2026-02-20'
  diff_url: https://github.com/gr2m/ai-provider-api-changes/pull/578
- change: added
  target: response
  breaking: false
  deprecated: false
  doc_only: false
  note: Added format constraint 'unixtime' to Eval.created_at.
  paths:
    - path: components/schemas/Eval/properties/created_at/format
      before: 'null'
      after: unixtime
  date: '2026-08-24'
- change: removed
  target: response
  breaking: false
  deprecated: false
  doc_only: false
  note: Removed default value from Eval.testing_criteria.
  paths:
    - path: components/schemas/Eval/properties/testing_criteria/default
      before: '"eval"'
      after: 'null'
  date: '2026-08-24'
- change: added
  target: response
  breaking: false
  deprecated: false
  doc_only: false
  note: Added 'max' as an allowed value for ReasoningEffort.
  paths:
    - path: components/schemas/ReasoningEffort/anyOf/0/enum
      before: '["none","minimal","low","medium","high","xhigh"]'
      after: '["none","minimal","low","medium","high","xhigh","max"]'
  date: '2026-08-24'
- change: changed
  target: response
  breaking: false
  deprecated: false
  doc_only: true
  note: Updated ReasoningEffort description and guidance/link.
  paths:
    - path: components/schemas/ReasoningEffort/anyOf/0/description
      before: |-
        Constrains effort on reasoning for
        [reasoning models](https://platform.openai.com/docs/guides/reasoning).
        Currently supported values are `none`, `minimal`, `low`, `medium`, `high`, and `xhigh`. Reducing
        reasoning effort can result in faster responses and fewer tokens used
        on reasoning in a response.

        - `gpt-5.1` defaults to `none`, which does not perform reasoning. The supported reasoning values for `gpt-5.1` are `none`, `low`, `medium`, and `high`. Tool calls are supported for all reasoning values in gpt-5.1.
        - All models before `gpt-5.1` default to `medium` reasoning effort, and do not support `none`.
        - The `gpt-5-pro` model defaults to (and only supports) `high` reasoning effort.
        - `xhigh` is supported for all models after `gpt-5.1-codex-max`.
      after: |-
        Constrains effort on reasoning for reasoning models. Currently supported
        values are `none`, `minimal`, `low`, `medium`, `high`, `xhigh`, and `max`.
        Reducing reasoning effort can result in faster responses and fewer tokens
        used on reasoning in a response. Not all reasoning models support every
        value. See the
        [reasoning guide](https://platform.openai.com/docs/guides/reasoning)
        for model-specific support.
  date: '2026-08-24'
- change: added
  target: response
  breaking: false
  deprecated: false
  doc_only: false
  note: Added optional prompt_cache_breakpoint to InputTextContent.
  paths:
    - path: components/schemas/InputTextContent/properties/prompt_cache_breakpoint
      before: 'null'
      after: '{"$ref":"#/components/schemas/PromptCacheBreakpointConfig"}'
  date: '2026-08-24'
- change: added
  target: response
  breaking: false
  deprecated: false
  doc_only: false
  note: Added new component PromptCacheBreakpointConfig.
  paths:
    - path: components/schemas/PromptCacheBreakpointConfig
      before: 'null'
      after: '{"description":"Marks the exact end of a reusable prompt prefix. The breakpoint inherits its TTL from the request''s `prompt_cache_options.ttl`; the boundary is not rounded to a token block.","properties":{"mode":{"default":"explicit","description":"The breakpoint mode. Always `explicit`.","enum":["explicit"],"type":"string"}},"required":["mode"],"title":"Prompt cache breakpoint","type":"object"}'
  date: '2026-08-24'
- change: added
  target: response
  breaking: false
  deprecated: false
  doc_only: false
  note: Added URI format to EvalItemInputImage.image_url.
  paths:
    - path: components/schemas/EvalItemInputImage/properties/image_url/format
      before: 'null'
      after: uri
  date: '2026-08-24'
