- change: added
  target: response
  breaking: false
  deprecated: false
  doc_only: false
  note: Added optional output_files property to ChatResponse to return files generated during the response.
  paths:
    - path: components/schemas/ChatResponse/properties/output_files
      before: 'null'
      after: '{"description":"Files generated during the response (e.g., by the code execution tool). Only populated when `code_execution_files_output` is included.","items":{"$ref":"#/components/schemas/OutputFile"},"type":["array","null"]}'
  date: '2026-03-06'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/752
- change: added
  target: response
  breaking: false
  deprecated: false
  doc_only: false
  note: Introduced new OutputFile schema referenced by ChatResponse.output_files.
  paths:
    - path: components/schemas/OutputFile
      before: 'null'
      after: '{"description":"A file generated during the model''s response (e.g., by the code execution tool).","properties":{"file_id":{"description":"The file ID from the Files API. Use this to download the file.","type":"string"},"name":{"description":"The display name of the file.","type":"string"}},"required":["file_id","name"],"type":"object"}'
  date: '2026-03-06'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/752
- change: added
  target: request
  breaking: false
  deprecated: false
  doc_only: false
  note: Added optional 'url' field to FileRef to allow public URL for file attachments.
  paths:
    - path: components/schemas/FileRef/properties/url
      before: 'null'
      after: '{"description":"Public URL for a file attachment.","type":["string","null"]}'
  date: '2026-03-17'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/792
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: true
  note: Updated ImageUrl.url description to mention it also accepts `image_url` for compatibility; no schema change.
  paths:
    - path: components.schemas.ImageUrl.properties.url.description
      before: URL of the image.
      after: URL of the image. Also accepts `image_url` for compatibility.
  date: '2026-03-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/823
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: true
  note: Fixed typo in ChatRequest.messages description ('the the' -> 'the').
  paths:
    - path: $.components.schemas.ChatRequest.properties.messages.description
      before: A list of messages that make up the the chat conversation. Different models support different message types, such as image and text.
      after: A list of messages that make up the chat conversation. Different models support different message types, such as image and text.
  date: '2026-03-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/826
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: true
  note: Clarified allowed values for ContentPart.type in description.
  paths:
    - path: $.components.schemas.ContentPart.properties.type.description
      before: The type of the content part.
      after: The type of the content part. Can be `text`, `image_url`, `text_file` or `file`.
  date: '2026-03-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/826
- change: removed
  target: request
  breaking: true
  deprecated: false
  doc_only: false
  note: Removed ContentPart.text_file from chat message content parts; use a file attachment or inline text instead.
  paths:
    - path: $.components.schemas.ContentPart.properties.text_file
      before: '{"description":"File path to a text file to be used as prompt.","type":["string","null"]}'
      after: 'null'
  date: '2026-04-02'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/860
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: false
  note: FileRef.file_id is now nullable (type changed from string to string|null).
  paths:
    - path: components/schemas/FileRef/properties/file_id/type
      before: string
      after: '["string","null"]'
  date: '2026-04-08'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/873
- change: removed
  target: request
  breaking: false
  deprecated: false
  doc_only: false
  note: Removed required constraint on FileRef; file_id is no longer required.
  paths:
    - path: components/schemas/FileRef/required
      before: '["file_id"]'
      after: 'null'
  date: '2026-04-08'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/873
- change: changed
  target: request
  breaking: true
  deprecated: false
  doc_only: false
  note: FileRef now requires file_id for file attachments; URL-only FileRef objects are no longer valid.
  paths:
    - path: components.schemas.FileRef.required
      before: 'null'
      after: '["file_id"]'
  date: '2026-04-08'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/874
- change: changed
  target: request
  breaking: true
  deprecated: false
  doc_only: false
  note: FileRef.file_id type is no longer nullable (changed from string|null to string).
  paths:
    - path: components.schemas.FileRef.properties.file_id.type
      before: '["string","null"]'
      after: '"string"'
  date: '2026-04-08'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/874
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: false
  note: FileRef.file_id is now optional and nullable; the required constraint was removed.
  paths:
    - path: components.schemas.FileRef.properties.file_id.type
      before: string
      after: '["string","null"]'
    - path: components.schemas.FileRef.required
      before: '["file_id"]'
      after: 'null'
  date: '2026-04-08'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/875
- change: added
  target: request
  breaking: false
  deprecated: false
  doc_only: false
  note: Added prompt_cache_key to ChatRequest for sticky routing/prompt caching.
  paths:
    - path: components/schemas/ChatRequest/properties/prompt_cache_key
      before: 'null'
      after: '{"type":["string","null"],"description":"A stable cache key for best-effort sticky routing / prompt-cache hits across requests sharing a prompt prefix. Plumbed to x-grok-conv-id, same as on /v1/responses."}'
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/929
- change: added
  target: request
  breaking: false
  deprecated: false
  doc_only: false
  note: Added service_tier to ChatRequest to select processing tier.
  paths:
    - path: components/schemas/ChatRequest/properties/service_tier
      before: 'null'
      after: '{"oneOf":[{"type":"null"},{"$ref":"#/components/schemas/ServiceTier"}],"description":"Specifies the processing tier for this request. Set to \"priority\" for higher scheduling priority at a higher token price. Valid values: \"auto\" (default), \"priority\"."}'
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/929
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: true
  note: 'Updated description for logprobs: newer models (grok-4.20+) no longer support it; field will be ignored.'
  paths:
    - path: components/schemas/ChatRequest/properties/logprobs/description
      before: Whether to return log probabilities of the output tokens or not. If true, returns the log probabilities of each output token returned in the content of message.
      after: Whether to return log probabilities of the output tokens or not. If true, returns the log probabilities of each output token returned in the content of message. Not supported by models `grok-4.20` and newer; the field will be silently ignored if set.
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/929
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: true
  note: Updated description for top_logprobs with note that grok-4.20+ ignores it.
  paths:
    - path: components/schemas/ChatRequest/properties/top_logprobs/description
      before: An integer between 0 and 8 specifying the number of most likely tokens to return at each token position, each with an associated log probability. logprobs must be set to true if this parameter is used.
      after: An integer between 0 and 8 specifying the number of most likely tokens to return at each token position, each with an associated log probability. logprobs must be set to true if this parameter is used. Not supported by models `grok-4.20` and newer; the field will be silently ignored if set.
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/929
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: true
  note: 'Updated description for reasoning_effort: only supported by grok-4.3; possible values now none, low (default), medium, high.'
  paths:
    - path: components/schemas/ChatRequest/properties/reasoning_effort/description
      before: Constrains how hard a reasoning model thinks before responding. Not supported by `grok-4` and will result in error if used with `grok-4`. Possible values are `low` (uses fewer reasoning tokens) and `high` (uses more reasoning tokens).
      after: Constrains how hard a reasoning model thinks before responding. Only supported by `grok-4.3`. Possible values are `none` (disables reasoning completely), `low` (this is the default if not specified), `medium` and `high` (uses the most reasoning tokens).
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/929
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: true
  note: 'Updated description for max_completion_tokens: default is now 128,000 when unset.'
  paths:
    - path: components/schemas/ChatRequest/properties/max_completion_tokens/description
      before: An upper bound for the number of tokens that can be generated for a completion, only applies to visible output tokens (i.e. does not apply to tokens used for reasoning or function calls). Defaults to None, meaning the model will generate as many tokens as needed up until the model's maximum context length.
      after: An upper bound for the number of tokens that can be generated for a completion, only applies to visible output tokens (i.e. does not apply to tokens used for reasoning or function calls). Defaults to 128,000 when unset; set a larger value to allow longer generations.
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/929
- change: added
  target: request
  breaking: false
  deprecated: false
  doc_only: false
  note: Added defer_loading to FunctionDefinition to support deferred tool definition loading.
  paths:
    - path: components/schemas/FunctionDefinition/properties/defer_loading
      before: 'null'
      after: '{"type":["boolean","null"],"description":"When true, the definition is hidden from the model''s prompt until loaded via a tool_search step. The tool stays callable at all times. Only supported on /v1/responses together with the tool_search tool."}'
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/929
- change: added
  target: request
  breaking: false
  deprecated: false
  doc_only: false
  note: ImageUrl now supports file_id (Files API) in addition to URL/base64.
  paths:
    - path: components/schemas/ImageUrl/properties/file_id
      before: 'null'
      after: '{"type":["string","null"],"description":"File ID from the xAI Files API. Mutually exclusive with `url`. The file must be an image (JPEG, PNG, or WebP) and fully uploaded."}'
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/929
- change: removed
  target: request
  breaking: false
  deprecated: false
  doc_only: false
  note: Removed required constraint on ImageUrl.url (either file_id or url may be provided).
  paths:
    - path: components/schemas/ImageUrl/required
      before: '["url"]'
      after: 'null'
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/929
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: true
  note: Expanded ImageUrl description to include base64 data URLs and Files API support.
  paths:
    - path: components/schemas/ImageUrl/description
      before: Image URL object of Image prompt
      after: Image input for generation and editing requests. Accepts a public URL, a base64-encoded data URL, or a file_id from the xAI Files API.
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/929
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: true
  note: Updated ImageUrl.url description to note base64 support and requirement when file_id is not set.
  paths:
    - path: components/schemas/ImageUrl/properties/url/description
      before: URL of the image. Also accepts `image_url` for compatibility.
      after: Public URL or base64-encoded data URL of the image (JPEG, PNG, or WebP). Also accepts `image_url` for compatibility. Required when `file_id` is not set.
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/929
- change: added
  target: response
  breaking: false
  deprecated: false
  doc_only: false
  note: Added service_tier to ChatResponse indicating the processing tier used.
  paths:
    - path: components/schemas/ChatResponse/properties/service_tier
      before: 'null'
      after: '{"$ref":"#/components/schemas/ServiceTier","description":"The processing tier used for this request."}'
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/929
- change: changed
  target: response
  breaking: false
  deprecated: false
  doc_only: false
  note: ChatResponse now requires service_tier in the response.
  paths:
    - path: components/schemas/ChatResponse/required
      before: '["id","object","created","model","choices"]'
      after: '["id","object","created","model","choices","service_tier"]'
  date: '2026-08-24'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/929
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: true
  note: Updated description of FunctionDefinition.defer_loading to clarify behavior and scope; no schema change.
  paths:
    - path: components.schemas.FunctionDefinition.properties.defer_loading.description
      before: |-
        When true, the definition is hidden from the model's prompt until loaded
        via a `tool_search` step. The tool stays callable at all times. Only
        supported on `/v1/responses` together with the `tool_search` tool.
      after: |-
        When true, the definition is hidden from the model's prompt but stays
        callable, loaded via a `tool_search` step (`/v1/responses` only).
  date: '2026-08-25'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/947
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: true
  note: Updated documentation for FunctionDefinition.defer_loading to clarify tool visibility/loading and support scope.
  paths:
    - path: components.schemas.FunctionDefinition.properties.defer_loading.description
      before: |-
        When true, the definition is hidden from the model's prompt but stays
        callable, loaded via a `tool_search` step (`/v1/responses` only).
      after: |-
        When true, the definition is hidden from the model's prompt until loaded
        via a `tool_search` step. The tool stays callable at all times. Only
        supported on `/v1/responses` together with the `tool_search` tool.
  date: '2026-08-25'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/948
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: true
  note: Documentation for FunctionDefinition.defer_loading clarified (still /v1/responses-only; remains callable while hidden). No schema changes.
  paths:
    - path: components/schemas/FunctionDefinition/properties/defer_loading/description
      before: |-
        When true, the definition is hidden from the model's prompt until loaded
        via a `tool_search` step. The tool stays callable at all times. Only
        supported on `/v1/responses` together with the `tool_search` tool.
      after: |-
        When true, the definition is hidden from the model's prompt but stays
        callable, loaded via a `tool_search` step (`/v1/responses` only).
  date: '2026-08-25'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/949
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: true
  note: Updated description of FunctionDefinition.defer_loading to clarify support and behavior.
  paths:
    - path: components/schemas/FunctionDefinition/properties/defer_loading/description
      before: |-
        When true, the definition is hidden from the model's prompt but stays
        callable, loaded via a `tool_search` step (`/v1/responses` only).
      after: |-
        When true, the definition is hidden from the model's prompt until loaded
        via a `tool_search` step. The tool stays callable at all times. Only
        supported on `/v1/responses` together with the `tool_search` tool.
  date: '2026-08-26'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/950
