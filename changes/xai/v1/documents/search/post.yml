- change: added
  target: request
  breaking: false
  deprecated: false
  doc_only: false
  note: Added optional rag_pipeline property to DocumentsSource to select RAG backend.
  paths:
    - path: components/schemas/DocumentsSource/properties/rag_pipeline
      before: 'null'
      after: '{"oneOf":[{"type":"null"},{"$ref":"#/components/schemas/RagPipeline","description":"Which RAG pipeline to use. Defaults to `chroma_db` when unset."}]}'
  date: '2026-03-08'
- change: added
  target: request
  breaking: false
  deprecated: false
  doc_only: false
  note: Introduced RagPipeline enum schema with values chroma_db and es.
  paths:
    - path: components/schemas/RagPipeline
      before: 'null'
      after: '{"enum":["chroma_db","es"],"type":"string"}'
  date: '2026-03-08'
