- change: added
  target: request
  breaking: false
  deprecated: false
  doc_only: false
  note: Added optional reference_images array (ImageUrl[]) for reference-to-video (R2V) generation.
  paths:
    - path: components/schemas/GenerateVideoRequest/properties/reference_images
      before: 'null'
      after: '{"description":"Optional reference images for reference-to-video (R2V) generation.\nWhen provided generates video using these images\nas style/content references.","items":{"$ref":"#/components/schemas/ImageUrl"},"type":"array"}'
  date: '2026-03-23'
  diff_url: https://github.com/gr2m/ai-provider-monitor/pull/817
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: true
  note: Updated duration description to mention `seconds` alias and acceptance of string values for compatibility.
  paths:
    - path: components/schemas/GenerateVideoRequest/properties/duration/description
      before: 'Video duration in seconds. Range: [1, 15]. Default: 8.'
      after: |-
        Video duration in seconds. Range: [1, 15]. Default: 8.
        Also accepts `seconds` for OpenAI API compatibility.
        Accepts both number (8) and string ("8") values.
  date: '2026-03-24'
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: true
  note: Updated image field description to accept `input_reference` alias for compatibility.
  paths:
    - path: components/schemas/GenerateVideoRequest/properties/image/oneOf/1/description
      before: |-
        Optional input image for image-to-video generation.
        If provided, generates video with this image as the first frame.
      after: |-
        Optional input image for image-to-video generation.
        If provided, generates video with this image as the first frame.
        Also accepts `input_reference` for compatibility (same semantics — first frame).
  date: '2026-03-24'
- change: changed
  target: request
  breaking: false
  deprecated: false
  doc_only: true
  note: Updated ImageUrl.url description to mention `image_url` alias for compatibility.
  paths:
    - path: components/schemas/ImageUrl/properties/url/description
      before: URL of the image.
      after: URL of the image. Also accepts `image_url` for compatibility.
  date: '2026-03-24'
