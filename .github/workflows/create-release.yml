name: Create Release on PR Merge

on:
  pull_request:
    types: [closed]
    branches: [main]

jobs:
  create-release:
    # Only run if the PR was merged (not just closed), and do not run from forks
    if: github.event.pull_request.merged == true && github.event.pull_request.base.repo.fork == false
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          fetch-depth: 0 # Fetch all history to access tags

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version-file: package.json
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - uses: actions/create-github-app-token@v2
        id: app-token
        with:
          app-id: ${{ vars.APP_ID }}
          private-key: ${{ secrets.APP_PRIVATE_KEY }}

      - name: Create release
        id: create-release
        env:
          GITHUB_TOKEN: ${{ steps.app-token.outputs.token }}
        run: node scripts/create-release.js
